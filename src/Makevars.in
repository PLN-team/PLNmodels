CXX_STD = CXX11
NLOPT_LIBS=@nlopt_libs@
PKG_CPPFLAGS = @nlopt_flag@
PKG_CXXFLAGS = @nlopt_flag@
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(NLOPT_LIBS)

all:

clean:
	rm -f $(SHLIB) $(OBJECTS)

