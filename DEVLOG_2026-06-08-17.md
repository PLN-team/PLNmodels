# Journal de développement — 8-9 juin 2026

Branche : `code-enhancement`

## Contexte

Refonte complète des optimiseurs pour le modèle PLN (Poisson Log-Normal, variational EM) et ses variantes de covariance (full, diagonal, sphérique, fixed). L'objectif est d'améliorer la qualité des optima trouvés et la vitesse de convergence.

---

## 1. Backend `homemade` — coordonnée-Newton maison (08/06)

**Fichiers** : `src/newton_impl.h`, `src/CovarianceTraits.h`, `src/newton_*.cpp`, `R/PLNfit-class.R`

### Ce qui a été fait

Implémentation d'un optimiseur coordonnée-Newton template (`newton_optimize_impl<Traits>`) remplaçant NLOPT comme backend principal pour les quatre variantes de covariance (full, diagonal, sphérique, fixed).

**Architecture** :
- `CovarianceTraits.h` : traits template (`FullCovTraits`, `DiagonalCovTraits`, `SphericalCovTraits`, `FixedCovTraits`) encapsulant les spécificités de chaque covariance (gradient/hessien de M, mise à jour de Σ, ELBO).
- `newton_impl.h` : template générique de l'algorithme, instancié par covariance.
- Paramétrage `log(S²)` pour S (évite les contraintes de positivité, meilleur conditionnement).

**Algorithme** (boucle EM externe + boucle interne Newton) :
- **Boucle interne** : step Newton diagonal pour M (avec Armijo), point fixe exact pour S² = `−1/(A + diag(Ω))`.
- **Boucle externe EM** : M-step analytique pour Σ (full/diag/sphérique) ; B mis à jour par Newton avec Hessien diagonal Poisson.
- **VE step** (`newton_vestep_impl`) : variante pour M et S seuls (B, Ω fixes), utilisée en validation croisée.

**Ajout** : `backend = "homemade"` dans `PLN_param()`, compatible avec tous les appels existants.

---

## 2. Backend spectral pour PLNPCA (09/06 matin)

**Fichiers** : `src/spectral_rank_cov.cpp`, `src/newton_rank_cov.cpp`

Implémentation d'un gradient spectral (méthode de Barzilai-Borwein avec acceptation GLL non-monotone) pour l'optimisation de PLNPCA (`homemade` backend). Compétitif avec NLOPT/CCSAQ, tolérance par défaut `1e-9` pour contrer les oscillations de l'acceptation non-monotone.

---

## 3. Paramétrisations alternatives et backend `homemade_alt` (09/06 après-midi)

**Fichiers** : `src/newton_impl_alt.h`, `src/newton_*_cov.cpp`, `R/PLNfit-class.R`, `R/PLN.R`

### Motivation

Dans la paramétrisation standard, M stocke le résidu `M_res = M_full − XB`, et B est mis à jour par Newton approché à chaque itération interne. La paramétrisation alternative stocke `M_full` et dispose d'une **forme close exacte pour B** dans le M-step :

```
B̂ = (X'WX)⁻¹ X'W M_full
```

### Ce qui a été fait

Nouveau template `newton_optimize_alt_impl<Traits>` dans `newton_impl_alt.h` :
- Boucle interne : M_full et S mis à jour (B gelé), gradient utilise `M_res = M_full − XB`.
- M-step : B par forme close, puis Σ/Ω analytique.
- Entrée/sortie en format résiduel (compatible avec le reste du package).
- Cas `fixed_cov` : itère (boucle interne + mise à jour B) jusqu'à convergence de l'ELBO.

**Nouvelles fonctions exportées** : `newton_optimize_full_alt`, `newton_optimize_diagonal_alt`, `newton_optimize_spherical_alt`, `newton_optimize_fixed_alt`.

**Ajout** : `backend = "homemade_alt"` dans `PLN_param()`.

### Problème cold-start

La variante `homemade_alt` seule souffre d'un problème de démarrage à froid pour les covariances full/diagonal : B est gelé pendant la boucle interne, puis fait un grand saut au M-step, désalignant M_full et XB_new.

---

## 4. Backend `hybrid` (09/06 après-midi)

**Fichiers** : `R/utils.R`, `R/PLNfit-class.R`

### Solution au cold-start

Fonction `make_hybrid_optimizer(opt1, opt2)` dans `utils.R` : chaîne deux optimiseurs.
- **Phase 1** (`homemade`, tolérance ×10) : converge rapidement vers le bon bassin.
- **Phase 2** (`homemade_alt`, tolérance cible) : raffine depuis ce bon point de départ.

Correction importante : `params2 <- modifyList(params, list(B=..., M=..., S=...))` pour passer Omega à travers dans le cas `fixed_cov`.

**Ajout** : `backend = "hybrid"` dans `PLN_param()`.

### Résultats sur `oaks` (n=116, p=114)

| Backend | ELBO full | Temps | ELBO diag | Temps |
|---|---|---|---|---|
| `homemade` | −32043 | 5.5s | −38411 | 4.1s |
| `homemade_alt` | froid | — | −38408 | 1.3s |
| **`hybrid`** | **−32033** | **3.0s** | **−38408** | **2.0s** |

`hybrid` est universellement meilleur que `homemade` seul : +4 à +15 ELBO selon la config, 1.3–2.7× plus rapide.

---

## 5. Paramétrisation alternative pour `fixed_cov` (09/06)

Extension naturelle de `homemade_alt` / `hybrid` à `PLNfit_fixedcov` (Ω fourni en entrée, non estimé). La boucle externe itère (boucle interne + B forme close) jusqu'à convergence de l'ELBO partiel. Correction de `make_hybrid_optimizer` pour transmettre Ω via `modifyList`.

---

## 6. Backend `nlopt` amélioré — B profilé analytiquement (09/06 soir)

**Fichiers** : `src/nlopt_full_cov.cpp`, `src/nlopt_diag_cov.cpp`, `src/nlopt_spherical.cpp`, `src/nlopt_fixed_cov.cpp`

### Idée

Plutôt qu'inclure B dans le vecteur de paramètres NLOPT, on le profil analytiquement à chaque évaluation du gradient :

```cpp
// Préconvergence une fois : P_X = solve(X'WX, X'W)   [d×n]
// À chaque eval NLOPT :
B     = P_X * M_full          // forme close O(d·n·p)
M_res = M_full - X * B
// gradient pour M_full = gradient pour M_res (théorème de l'enveloppe)
```

Le vecteur de paramètres passe de `(B, M, S)` taille `n(d+2p)` à `(M_full, S)` taille `2np`. Le paysage de l'objectif est plus lisse (B toujours à son optimum conditionnel).

**Implémentations** :
- `nlopt_optimize_full` : boucle EM conservée (inv_sympd coûteux), B profilé dans le E-step.
- `nlopt_optimize_diagonal` / `nlopt_optimize_spherical` : pas de boucle EM, B **et** σ² profilés à chaque eval.
- `nlopt_optimize_fixed` : pas de M-step Σ, B profilé à chaque eval.

Les anciennes implémentations (B dans le vecteur NLOPT) sont archivées sous le suffixe `_old` dans le code C++, non exportées vers R.

### Résultats sur `oaks`

| Backend | ELBO full | ELBO diag | ELBO sphér. | Temps diag |
|---|---|---|---|---|
| `nlopt` (ancien) | −32183 | −38439 | −39456 | 2.9s |
| **`nlopt` (nouveau)** | **−32060** | **−38408** | **−39450** | **0.7s** |
| `hybrid` | −32033 | −38408 | −39450 | 2.0s–3.0s |

Gains : +122 ELBO (full), +31 ELBO (diagonal), **4× plus rapide** (diagonal), **3× plus rapide** (sphérique et fixed).

---

---

## 7. Corrections et factorisation des helpers nlopt (10/06)

**Fichiers** : `src/nlopt_full_cov.cpp`, `src/nlopt_diag_cov.cpp`, `src/nlopt_spherical.cpp`

### Factorisation du calcul objectif/gradient

Les fonctions `nlopt_optimize_*` et `nlopt_optimize_vestep_*` partageaient un calcul objectif/gradient identique à un terme de pénalité et un Z près. On a factorisé ce code dans des helpers statiques :

- `full_cov_obj_grad_impl(M_res, Z, logS2, Omega, ...)` : commun à E-step et vestep full.
- `diag_cov_obj_grad_impl(M_res, Z, S2, logS2, inv_sigma2, penalty, ...)` : commun diagonal.
- `spherical_cov_obj_grad_impl(M_res, Z, S2, logS2, inv_sigma2, penalty, ...)` : commun sphérique.

Le `penalty` et le `Z` sont pré-calculés par l'appelant, qui seul connaît si on est en E-step profilé ou en vestep (B fixe).

### Correction de bug : vestep sphérique

**Bug pré-existant** dans `nlopt_optimize_vestep_spherical` : le gradient utilisait `M / omega2` au lieu de `M * omega2` (division par la précision = multiplication par la variance, direction opposée). La pénalité dans l'objectif (`omega2 * accu(...)`) était correcte, seul le gradient était faux. Corrigé en passant par le helper qui utilise `M_res * inv_sigma2 = M * omega2`.

---

## 8. Piste explorée (abandonnée) : Hessienne complète pour le step Newton (10/06)

**Question initiale** : `homemade_alt` avec B gelé converge vers des optima moins bons que `homemade` (écart de 120 unités ELBO sur `oaks`). Peut-on améliorer le step Newton en utilisant la Hessienne complète plutôt que la diagonale ?

### Analyse théorique

La Hessienne exacte par échantillon est (Eq. 19 du document `alter_param.pdf`) :

```
H_M_i = −w_i · (diag(A_i) + Ω)   [p×p]
```

Pour les covariances diagonale et sphérique, Ω est diagonal : la Hessienne complète se réduit à la diagonale déjà utilisée — **aucun gain possible**.

Pour la covariance full, les éléments hors-diagonale de Ω sont ignorés par l'approximation diagonale. Utiliser la Hessienne exacte nécessiterait un solve p×p par échantillon : coût O(np³) ≈ 170M flops par itération sur `oaks` (n=116, p=114), contre O(np) actuellement.

**Estimation du rapport coût/bénéfice** (sur `oaks`) :
- Coût step exact : ~1000× plus cher par itération
- Gain en itérations : ~10× (Newton quadratique vs linéaire)
- Bilan : ~100× plus lent. **Non rentable.**

### Mesures effectuées

Comptage des itérations sur `oaks` (n=116, p=114) avec budget=50 EM × 50 inner :

| Backend | Itérations internes | ELBO |
|---|---|---|
| `homemade_alt` | 657 (~13/EM step) | −31 522 |
| `homemade` | 3 136 (~63/EM step) | −31 402 |
| `hybrid` | 1 076 | −31 402 |

La vrai cause du mauvais ELBO de `homemade_alt` n'est pas le step Newton mais le **B gelé** : l'optimum trouvé est sous-optimal parce que la boucle interne converge vite vers le meilleur M_full *pour le B courant*, puis B saute au M-step, désalignant tout.

---

## 9. Théorème de l'enveloppe dans la boucle interne (10/06)

**Fichier** : `src/newton_impl_alt.h`

### Idée

Dans `nlopt_optimize_full`, B est mis à jour à **chaque évaluation de gradient** :
```cpp
B     = P_X * M_full   // P_X = (X'WX)⁻¹X'W, précomputé
M_res = M_full − X * B
// gradient pour M_full ≡ gradient pour M_res (théorème de l'enveloppe)
```

Ce profil continu de B élimine le cold-start. On applique la même idée dans la boucle interne de `homemade_alt`.

### Implémentation

`P_X = solve(XtWX, Xw')` précomputée une fois. À chaque pas Newton :

1. `B = P_X * M_full` — B mis à jour au début de l'itération
2. `XB = X * B` — gelé pendant la recherche de ligne (cohérent avec nlopt qui évalue le gradient une fois par pas quasi-Newton)
3. `M_res = M_full − XB`
4. Gradient/Hessien calculés sur `M_res` (formule inchangée par théorème de l'enveloppe)
5. Step Newton `step_M = grad_M / hess_M`

### Première tentative : Armijo avec XB gelé

Résultats initiaux sur `oaks` (budget=300) : ELBO = −32 029 (vs −32 031 pour `homemade`). Amélioration sur `oaks`. Mais sur `trichoptera` (d≈15, n=49) : ELBO = −897 vs −841 pour `homemade`. **Régression sévère sur les datasets avec beaucoup de covariables.**

**Cause** : avec XB gelé, l'Armijo évalue l'objectif en `M_res − α·step_M` (le pas complet). Mais avec B live, le vrai `M_res` après le pas est `M_res − α·Q_step` où `Q_step = (I − X·P_X)·step_M`. Quand d/n est grand (trichoptera : d≈15, n=49), la composante X·P_X·step_M est importante : l'Armijo acceptait des pas bien trop larges, menant à une convergence oscillante et de mauvaise qualité.

### Correction : pente projetée Q_step dans l'Armijo

Le changement réel de `M_res` pour un pas `−α·step_M` sur M_full est :

```
ΔM_res = −α · Q_step = −α · (I − X·P_X) · step_M
```

On corrige l'Armijo pour évaluer l'objectif au bon point :

```cpp
const arma::mat Q_step  = step_M - X * (P_X * step_M);       // changement réel de M_res
arma::mat QstepO = Traits::times_Omega(Q_step, state);        // précomputé une fois
double slope_M   = -arma::accu(grad_M % Q_step);              // pente projetée correcte
// fallback si non-descente (cas dégénéré d/n grand) :
if (slope_M >= 0) slope_M = -arma::accu(grad_M % step_M);

// Armijo loop : MresOt = MresO - α * QstepO  (O(np), sans recalculer B)
//               MresT  = M_res - α * Q_step
```

### Résultats après correction (budget maxit=max_em=100)

| Dataset | homemade (ancien) | homemade_alt corrigé | hybrid |
|---|---|---|---|
| oaks (d=1, n=116, p=114) | −32 034, 2 077 it | −32 030, 1 077 it | −32 029, 1 001 it |
| trichoptera (d≈15, n=49, p=17) | −844, 3 538 it | **−840**, 8 256 it | **−840**, 8 691 it |
| barents (d=5, n=89, p=30) | −4 379, 3 438 it | **−4 304**, 5 594 it | **−4 304**, 5 764 it |

Avec la correction Q_step, `homemade_alt` est maintenant **équivalent ou supérieur** à l'ancien `homemade` sur tous les datasets. L'Armijo correct empêche les pas surdimensionnés : plus d'itérations par pas EM (82 vs 10 sur trichoptera), mais chaque itération est productive.

---

## 10. Refactorisation finale : suppression de `homemade`, renommage (10/06)

**Fichiers** : `src/newton_{full,diag,spherical,fixed}_cov.cpp`, `R/PLNfit-class.R`, `R/PLN.R`, `R/utils.R`

### Décision

Le benchmark sur trois datasets (oaks, trichoptera, barents) montre que `homemade_alt` corrigé **domine l'ancien `homemade`** en qualité ELBO et en nombre d'itérations, à budget égal :

- meilleur ELBO sur barents et trichoptera
- equivalent ou légèrement meilleur sur oaks
- 2–4× moins d'itérations totales

L'ancien `homemade` est supprimé. `homemade_alt` devient le nouveau `homemade`.

### Changements C++

Les quatre fichiers `newton_*_cov.cpp` suppriment la fonction basée sur `newton_optimize_impl` et renomment `newton_optimize_*_alt` → `newton_optimize_*`. `newton_impl.h` est conservé (utilisé par `newton_vestep_impl` pour les vesteps). `RcppExports.{R,cpp}` régénérés par `Rcpp::compileAttributes()`.

### Nouveau `hybrid` : nlopt → new homemade

L'ancien `hybrid` chaînait `homemade → homemade_alt`, les deux ayant le même type de config. Le nouveau `hybrid` chaîne :

- **Phase 1** : `nlopt_optimize_*` (CCSAQ, tolérance 10× relâchée) — exploration globale rapide
- **Phase 2** : `newton_optimize_*` (nouveau homemade, tolérance cible) — raffinement local Newton

`make_hybrid_optimizer` construit explicitement un config nlopt pour la phase 1 (depuis `config_default_nlopt`), plutôt que de passer le config homemade. Cela évite l'erreur `algorithm="NEWTON" non reconnu par nlopt`.

En pratique, `hybrid` et `homemade` convergent désormais au même ELBO (à ±0.1 unité) sur tous les datasets testés. `hybrid` reste utile comme filet de sécurité pour les paysages difficiles où le point d'initialisation importe.

---

## 11. Correction des bugs `ifelse()` dans PLNLDAfit (10/06)

**Fichier** : `R/PLNLDAfit-class.R`

### Bug

`PLNLDAfit_diagonal$initialize` et `PLNLDAfit_spherical$initialize` utilisaient `ifelse()` au lieu de `if/else` pour sélectionner l'optimiseur :

```r
# Code bugué
private$optimizer$main <- ifelse(control$backend == "nlopt",
                                 nlopt_optimize_diagonal,
                                 private$torch_optimize)
```

`ifelse()` évalue les **deux branches** et retourne un scalaire. Avec `backend = "homemade"` (désormais défaut), la condition est `FALSE` → retourne `private$torch_optimize`. Le torch optimizer échoue avec `double(length = config$num_epoch + 1)` car `config_default_homemade` n'a pas de champ `num_epoch`.

### Correction

Remplacement par un bloc `if/else if/else` complet dans les deux classes, avec dispatch correct pour les quatre backends (`torch`, `homemade`, `hybrid`, `nlopt`) et mise à jour du vestep pour les backends Newton :

```r
private$optimizer$main <- if (control$backend == "torch") {
  private$torch_optimize
} else if (control$backend == "homemade") {
  newton_optimize_diagonal       # ou _spherical
} else if (control$backend == "hybrid") {
  make_hybrid_optimizer(nlopt_optimize_diagonal, newton_optimize_diagonal)
} else {
  nlopt_optimize_diagonal
}
private$optimizer$vestep <- if (control$backend %in% c("homemade", "hybrid"))
  newton_optimize_vestep_diagonal else nlopt_optimize_vestep_diagonal
```

---

## 12. Alignement des backends pour PLNnetwork et PLNmixture (10/06)

**Fichiers** : `R/PLNnetwork.R`, `R/PLNmixture.R`

### Constats

- `PLNnetwork_param()` et `PLNmixture_param()` proposaient uniquement `"nlopt"` et `"torch"`, bloquant `"homemade"` et `"hybrid"` par un `stopifnot()` redondant.
- `PLNmixture_param()` référençait `config_default_nlopt_pln` (objet inexistant) → crash immédiat à l'appel.

### Corrections apportées

Les deux `_param()` acceptent maintenant `c("homemade", "nlopt", "hybrid", "torch")` avec suppression du `stopifnot` redondant et ajout de la branche `else { config_opt <- config_default_homemade }`.

### Choix du défaut : analyse de la boucle externe de PLNnetwork

La question clé : faut-il choisir `"homemade"` comme défaut pour PLNnetwork (comme pour PLN) ?

**Test expérimental** (trichoptera, 3 pénalités) :

| Backend | Itérations outer | Temps |
|---|---|---|
| `nlopt` | 17 + 6 + 9 = **32** | 0.85 s |
| `homemade` | 20 + 18 + 20 = **58** (hits maxit_out) | 1.06 s |

Avec `homemade`, beaucoup de pénalités atteignent `maxit_out=20` sans converger. Cause : le Newton avec enveloppe optimise très à fond à chaque appel (M, S, B jusqu'à ftol=1e-8), ce qui provoque des changements importants de Σ = M'M/n + diag(S²) à chaque étape, rendant le schéma d'alternance glasso ↔ PLN lent à converger globalement. En revanche, sur `PLN(covariance="fixed")` seul, `homemade` est plus rapide que `nlopt` (0.76s vs 1.01s sur oaks). C'est un **problème d'articulation**, pas de performance intrinsèque.

**Décision** : PLNnetwork conserve `"nlopt"` comme défaut. PLNmixture utilise `"homemade"` (ses composantes sont des PLNfit standards, l'alternance EM–Mixture est différente).

---

## 13. Correction : warning `solve()` sur formule sans covariable (10/06)

**Fichier** : `src/newton_impl_alt.h`

### Bug

`PLN(Abundance ~ 0, data = ...)` (ou toute formule sans covariable) déclenche :

```
warning: solve(): system is singular; attempting approx solution
```

Cause : avec `d = 0`, `X` est une matrice n×0, `XtWX = X'WX` est une matrice 0×0, et `arma::solve(0×0, ...)` émet ce warning.

### Correction

Garde sur la taille de X avant l'appel à `solve` :

```cpp
const arma::mat P_X = (X.n_cols > 0)
    ? arma::solve(XtWX, Xw.t())
    : arma::mat(0, n);   // d=0 : P_X vide, toutes les opérations avec B sont des no-ops
```

Toutes les opérations matricielles suivantes (produits avec X, B, P_X) produisent les bonnes valeurs nulles quand d=0 grâce à l'arithmétique Armadillo sur matrices vides.

**Note** : le touch des `.cpp` incluant `newton_impl_alt.h` est nécessaire pour forcer la recompilation du header.

---

## 14. Nettoyage : code mort (10/06)

**Fichiers** : `R/utils.R`, `R/PLNfit-class.R`, `R/PLN.R`, `R/plot_utils.R`, `R/PLNnetwork.R`, `src/nlopt_wrapper.h`, `src/nlopt_wrapper.cpp`

Audit complet du code mort côté R et C++. Suppressions :

**R :**
- `utils.R` : `status_to_message()` (jamais appelée ; seul usage dans un commentaire de `PLNfit-class.R`), `trace()` (shadow du `base::trace`, aucun appel actif), `node_pair_to_egde()` (typo + jamais appelée, contrairement à `edge_to_node`)
- `PLNfit-class.R` : classe `PLNfit_genetprior` entière (~100 lignes commentées) — modèle à covariance génétique abandonné
- `PLN.R` : branche `# "genet" = PLNfit_$new(...)` (classe inexistante)
- `plot_utils.R` : `GeomCircle` + `geom_circle()` (~45 lignes, jamais appelées)
- `PLNnetwork.R` : champ orphelin `variance = TRUE` dans la liste retournée par `PLNnetwork_param()`

**C++ :**
- `nlopt_wrapper.h` + `nlopt_wrapper.cpp` : déclarations et corps des 4 fonctions helper commentées (`set_uniform_x_weights`, `set_per_value_x_weights`, `set_uniform_xtol_abs`, `set_per_value_xtol_abs`) + leurs appels commentés dans le test

**Corrections mineures :**
- `utils.R` : typo `w <-- rep(...)` → `w <- rep(...)` dans l'exemple `compute_PLN_starting_point`
- `PLN.R` : `@param backend` mis à jour (défaut `"homemade"`, `"hybrid"` ajouté)
- `plot_utils.R` : bug `center[1]` → `center[2]` dans la coordonnée y de `circle()` (inoffensif car toujours appelée avec `c(0,0)`)

---

## 15. Activation de `PLNLDA_param()` (10/06)

**Fichiers** : `R/PLNLDA.R`, `tests/testthat/test-plnlda-fit.R`

### Situation initiale

`PLNLDA_param()` était définie, exportée et documentée, mais `PLNLDA()` utilisait `PLN_param()` comme défaut. Les deux fonctions retournent un `PLNmodels_param` compatible, avec une différence : `PLNLDA_param()` n'expose pas l'option `"fixed"` ni le paramètre `Omega`, non pertinents en LDA.

De plus, `test-plnlda-fit.R` passait `PLNLDA_param(covariance = "diagonal/spherical")` à `PLN()` (bug de test).

### Corrections

- `PLNLDA()` : signature changée en `control = PLNLDA_param()`; documentation et message d'erreur mis à jour.
- `test-plnlda-fit.R` : `PLNLDA_param` → `PLN_param` dans les deux lignes qui testaient `PLN()`.

---

## 16. Audit global du code — branche `code-enhancement` vs `master` (10/06)

Audit multi-angles (7 finders indépendants + 10 verifiers) sur le diff complet de la branche. Méthodologie : line-by-line, removed-behavior, cross-file, reuse, simplification, efficiency, altitude — chaque finding soumis à un verifier avant confirmation.

8 findings confirmés ou plausibles, classés par sévérité :

| # | Sévérité | Fichier | Résumé |
|---|---|---|---|
| 1 | Critique | `PLNfit-class.R` | `PLNfit_fixedcov$initialize` appelle `setup_optimizer` sans les args vestep → `optimizer$vestep = NULL` → crash dans `predict(level=1)` et `predict_cond()` |
| 2 | Critique | `PLNmixturefamily-class.R` | `config_fast$maxit_out <- 2` mais `optimize()` lit `config$maxit_em` → smoothing tourne 50 EM iters au lieu de 2 (~25× plus lent) |
| 3 | Haute | `PLNfit-class.R` | `do.call(compute_PLN_starting_point, data)` dans jackknife/bootstrap → sensitive aux noms des éléments de `data`, échoue si `method` est dans la liste |
| 4 | Haute | `PLNfit-class.R` | `predict(level=1)` : M retourné par C++ sans colnames → noms d'espèces perdus dans la prédiction |
| 5 | Haute | `PLNmixturefit-class.R` | `latent_pos` active binding retourne `mix_up('var_par$M')` (M_full) au lieu de `mix_up('latent_pos')` (M_res) → k-means de clustering sur les mauvaises coordonnées |
| 6 | Moyenne | `utils.R` | `compute_PLN_starting_point` : `Y` non protégé contre les NA → crash si Y contient des NA |
| 7 | Moyenne | `utils.R` | `compute_PLN_starting_point` : `singular.ok = FALSE` rend le guard `B[is.na(B)] <- 0` inaccessible |
| 8 | Faible | `utils.R` | `extract_model` : `is.symbol(call$formula)` ne couvre pas les expressions de type call (ex. `formula(paste(...))`) |

Deux findings refutés (P6, P8 dans la numérotation interne) : `PLNLDAfit$predict` double-comptage — infirmé par la sémantique M_res + group_means ; `PLNmixturefit$predict` position — infirmé, le VE optimizer retourne déjà M_full.

---

## 17. Application des 8 corrections (10/06)

**Fix 1 — `PLNfit_fixedcov` vestep manquant** (`R/PLNfit-class.R`)

`setup_optimizer` appelé avec seulement 3 args au lieu de 5 : les fonctions vestep (`nlopt_optimize_vestep_full`, `newton_optimize_vestep_full`) n'étaient pas passées, laissant `optimizer$vestep = NULL`. `predict(level=1)` et `predict_cond()` crashaient via `do.call(NULL, args)`.

```r
# Avant
private$setup_optimizer(control$backend, nlopt_optimize_fixed, newton_optimize_fixed)
# Après
private$setup_optimizer(control$backend, nlopt_optimize_fixed, newton_optimize_fixed,
                        nlopt_optimize_vestep_full, newton_optimize_vestep_full)
```

**Fix 2 — smoothing PLNmixture : clé `maxit_em` au lieu de `maxit_out`** (`R/PLNmixturefamily-class.R`)

Dans `add_one_cluster()` et `remove_one_cluster()`, `config_fast$maxit_out <- 2` n'avait aucun effet car `optimize()` lit `config$maxit_em`. Le screening rapide des candidats tournait donc à plein régime (50 EM iters par candidat).

```r
# Avant (dans les deux fonctions)
config_fast$maxit_out <- 2
# Après
config_fast$maxit_em <- 2
```

**Fix 3 — jackknife/bootstrap : `do.call` supprimé** (`R/PLNfit-class.R`)

`do.call(compute_PLN_starting_point, data)` — `data` est une liste nommée `(Y, X, O, miss, w, formula)` dont les noms ne correspondent pas aux paramètres de `compute_PLN_starting_point(Y, X, O, w, method)`. Remplacé par un appel positionnel direct qui passe correctement `w` et permet de spécifier `method`.

**Fix 4 — colnames M dans `predict(level=1)`** (`R/PLNfit-class.R`)

L'optimiseur C++ retourne M sans attributs de noms. `colnames(M) <- colnames(private$B)` ajouté après récupération du résultat VE.

**Fix 5 — `latent_pos` de `PLNmixturefit`** (`R/PLNmixturefit-class.R`)

`mix_up('var_par$M')` accédait au champ brut `private$M` de chaque composante (M_full = μ_k + M_res). Le k-means dans `add_one_cluster()` opérait donc sur les coordonnées incluant les effets de groupe, biaisant la séparation. Corrigé en `mix_up('latent_pos')` qui appelle le binding actif de chaque `PLNfit` (M − X*B = M_res).

**Fix 6+7 — `compute_PLN_starting_point` : NA, singular.ok, pois_fam** (`R/utils.R`)

- `Y0 <- replace(Y, is.na(Y), 0)` : protection NA pour `lm.fit` et `glm.fit`
- `expO <- exp(O)` : calculé une seule fois (était calculé deux fois)
- `singular.ok = TRUE` : active la garde `B[is.na(B)] <- 0` (auparavant inaccessible car `lm.fit` plantait avant de retourner NA)
- `pois_fam <- poisson()` : famille créée une fois hors du `vapply` (évite p réallocations)

**Fix 8 — `extract_model` : `!inherits` au lieu de `is.symbol`** (`R/utils.R`)

`is.symbol(call$formula)` ne couvrait pas les expressions de type `call` (ex. `formula(paste(...))`), laissant l'attribut `xlevels` être assigné à un objet non-formula. Remplacé par `!inherits(call$formula, "formula")` qui couvre tous les cas non-formula.

---

## 18. Nettoyage de la suite de tests (10/06)

**Fichiers** : `tests/testthat/test-pln.R`, `tests/testthat/test-plnlda-fit.R`, `tests/testthat/test-standard-error.R`

### Test cassé par Fix 7 (`singular.ok = TRUE`)

`test-pln.R` lignes 209–225 attendait `expect_error(PLN(Y ~ X_singular))`. Après Fix 7, PLN gère silencieusement les designs singuliers (coefficients à 0 via `B[is.na(B)] <- 0`) au lieu de planter. Le test est mis à jour :

- Description : `"PLN: singular covariate model matrix is handled gracefully"`
- `expect_error` → `expect_no_error` pour les deux cas (covariables continues dupliquées, facteurs corrélés)

### Faux label + doublon (`test-pln.R` lignes 72–95)

Les deux blocs "diagonal" et "spherical" utilisaient `covariance = "spherical"` : code identique, couverture identique. Le bloc "diagonal" est corrigé pour utiliser réellement `covariance = "diagonal"` (ajout d'une couverture manquante). La variable `model3` (poids aléatoires sans `expect_*`) dans le bloc spherical est supprimée.

### Duplicate cross-fichier (`test-plnlda-fit.R` lignes 103–122)

Le test "PLNLDA fit: Check number of parameters" testait `PLN()` (copie quasi-identique de `test-plnfit.R:239`). Remplacé par un vrai test de `PLNLDA$nb_param` : Σ (`p*(p+1)/2`) + group means (`p*g`) + éventuels régresseurs.

### Noms de tests dupliqués (`test-standard-error.R` lignes 151, 159)

Deux blocs portaient le nom identique `"Check that variance estimation are coherent in PLNnetwork"`. Le premier testait en réalité PLNPCA. Renommé en `"Check that variance estimation are coherent in PLNPCA"`.

---

## Récapitulatif des backends disponibles

| `backend` | Algorithme | Défaut pour |
|---|---|---|
| `"homemade"` | Newton diagonal, B profilé (enveloppe), Armijo Q_step | PLN, PLNLDA, PLNmixture |
| `"nlopt"` | CCSAQ/NLOPT, B profilé (enveloppe) | PLNnetwork |
| `"hybrid"` | nlopt (exploration) → homemade (raffinement) | — |
| `"torch"` | RPROP/ADAM/... | — |

---

## Fichiers créés ou significativement modifiés

| Fichier | Nature |
|---|---|
| `src/CovarianceTraits.h` | Traits template pour les 4 covariances |
| `src/newton_impl.h` | Template coordonnée-Newton (vestep uniquement désormais) |
| `src/newton_impl_alt.h` | Template Newton diagonal avec B profilé (→ nouveau `homemade`) |
| `src/newton_{full,diag,spherical,fixed}_cov.cpp` | Instances + fonctions exportées (sans variantes `_alt`) |
| `src/nlopt_{full,diag,spherical,fixed}_cov.cpp` | B profilé + helpers statiques + bug fix vestep sphérique |
| `src/spectral_rank_cov.cpp` | Gradient spectral BB+GLL pour PLNPCA |
| `src/newton_rank_cov.cpp` | Coordonnée-Newton pour PLNPCA |
| `R/PLNfit-class.R` | Branchement backends dans les 4 classes PLNfit ; `PLNfit_genetprior` supprimée |
| `R/PLNLDAfit-class.R` | Correction `ifelse()` → `if/else` dans diagonal et sphérique |
| `R/PLN.R` | `PLN_param()` — `homemade_alt` supprimé ; doc backend mise à jour |
| `R/PLNLDA.R` | `PLNLDA()` utilise désormais `PLNLDA_param()` comme défaut |
| `R/PLNnetwork.R` | `"homemade"`/`"hybrid"` ajoutés ; défaut conservé à `"nlopt"` |
| `R/PLNmixture.R` | `"homemade"`/`"hybrid"` ajoutés ; défaut `"homemade"` ; bug `config_default_nlopt_pln` corrigé |
| `R/utils.R` | `make_hybrid_optimizer()` revu ; code mort supprimé |
| `R/plot_utils.R` | `GeomCircle`/`geom_circle` supprimés ; bug `center[1]` corrigé |
| `src/nlopt_wrapper.h` | Déclarations commentées supprimées |
| `src/nlopt_wrapper.cpp` | Corps commentés + appels orphelins supprimés |

---

## 19. Audit C++ — corrections appliquées (10/06)

Audit complet du code C++ : 8 améliorations identifiées (★★★ bugs/efficacité, ★★ factorisation, ★ style) et intégralement appliquées.

### Corrections ★★★

**Fix 1 — Guard P_X pour d=0 (`nlopt_*.cpp`)** : quand la formule est `~ 0` (sans covariables), `X` est une matrice (n×0) et `arma::solve(X.t() * Xw, Xw.t())` déclenchait un avertissement Armadillo. Corrigé dans les 4 fichiers :
```cpp
// Avant (tous les fichiers nlopt) :
const arma::mat P_X = arma::solve(X.t() * Xw, Xw.t());
// Après :
const arma::mat P_X = (X.n_cols > 0) ? arma::solve(X.t() * Xw, Xw.t()) : arma::mat(0, Y.n_rows);
```

**Fix 2 — O(p³) → O(np) dans `nlopt_fixed_cov.cpp`** : l'objectif utilisait `trace(Omega * (...))` coûtant O(p³), remplacé par le calcul élément-par-élément d'O(np) via `full_cov_obj_grad_impl` (déjà utilisée dans `nlopt_full_cov.cpp`). Gain asymptotique pour grand p.

**Fix 3 — Factorisation `FullCovTraits`/`FixedCovTraits`** : ces deux traits partageaient 6 méthodes statiques identiques (~35 lignes de duplication). Nouveau `DenseOmegaImpl` base struct avec les 6 méthodes ; `FullCovTraits` et `FixedCovTraits` en héritent. Seuls `mstep`, `elbo_cov`, `output_cov`, et `has_em` restent spécialisés.

### Corrections ★★

**Fix 4 — `NewtonConfig` dans `utils.h`** : struct centralisant l'extraction de config depuis `Rcpp::List` (pattern `containsElementNamed` répété 4 fois × 4 fichiers → 1 struct).

**Fix 5 — Adoption de `NewtonConfig`** : les 4 fichiers `newton_*.cpp` + `nlopt_full_cov.cpp` utilisent désormais `const NewtonConfig cfg(config);` au lieu du bloc 4-lignes dupliqué.

**Fix 6 — `nlopt_impl.h`** : nouveau header partagé exposant les 3 helpers `inline` (`full_cov_obj_grad_impl`, `diag_cov_obj_grad_impl`, `spherical_cov_obj_grad_impl`). Les copies `static` locales ont été supprimées des 3 fichiers `nlopt_*.cpp` qui les dupliquaient.

**Fix 7 — Méthode `update()` dans chaque `State`** : les constructeurs et `mstep` des 3 traits à covariance estimée (Full, Diagonal, Sphérique) contenaient un code identique. Ajout d'une méthode `update(M, S2, w, w_bar)` à chaque `State` ; le constructeur et `mstep` délèguent vers elle.

### Correction ★

**Fix 8 — `SphericalCovTraits::output_cov`** : `Sigma_out.diag() = arma::ones<arma::vec>(p) * s.sigma2` remplacé par `Sigma_out.diag().fill(s.sigma2)` (supprime la construction inutile d'un vecteur temporaire de 1s).

### Résultat

- Compilation propre (0 erreur, 0 nouveau warning)
- 72 tests PLN passent (FAIL 0)

---

# Journal de développement — 11-12 juin 2026

Branche : `code-enhancement`

---

## 20. Pas Newton conjoint sur (M, ψ) — remplacement du point fixe (11/06)

**Fichiers** : `src/CovarianceTraits.h`, `src/newton_impl.h`

### Motivation

L'ancienne boucle interne faisait :
1. Step Newton diagonal pour M (avec Armijo)
2. Point fixe exact pour S² : `S² = -1 / (A + diag(Ω))` (pas d'Armijo, convergence immédiate)

Cette séparation ignore le couplage entre M et S (via A = exp(M + S²/2 + O)). La correction de l'un modifie A, qui invalide l'autre. On travaille alternativement sur deux sous-problèmes couplés, ce qui ralentit la convergence.

### Nouveau pas conjoint (M, ψ) avec terme croisé

On paramétrise ψ = log(S²) et on résout simultanément le système 2×2 **par entrée (i,j)** :

```
Hessienne diagonale par blocs (indépendance entre (i,j)) :
  H_MM  = w·(A + ω_jj)
  H_ψψ  = 0.5·w·S²·(A·(1 + 0.5·S²) + ω_jj)
  H_Mψ  = 0.5·w·A·S²                              ← terme croisé (nul si A≈0)

det = H_MM·H_ψψ - H_Mψ²   (clampé à 1e-20)
step_M   = (H_ψψ·grad_M   - H_Mψ·grad_ψ) / det
step_ψ   = (H_MM·grad_ψ   - H_Mψ·grad_M) / det
```

L'Armijo est appliqué conjointement sur `(M - α·Q_step, ψ - α·step_ψ)` avec pente :

```
slope = -accu(grad_M % Q_step) - accu(grad_ψ % step_ψ)
```

où `Q_step = step_M - X * (P_X * step_M)` est la projection hors col(X) pour le B profilé.

### Résultat

Convergence plus robuste sur les grands datasets (microcosm, scRNA). Le terme croisé H_Mψ est faible quand A est petit (régions creuses), fort quand A est grand (signaux forts) — l'approximation diagonale par blocs est exacte dans la limite d'observations indépendantes.

---

## 21. Investigation et suppression de `block_newton_thresh` (12/06)

**Fichiers** : `src/CovarianceTraits.h`, `src/utils.h`, `src/newton_impl.h`, `src/newton_full_cov.cpp`, `src/newton_fixed_cov.cpp`, `R/utils.R`, `R/PLN.R`

### Origine du paramètre

Avant le pas conjoint, `DenseOmegaImpl` offrait un mode alternatif : pour `p ≤ block_newton_thresh`, on calculait le step exact par **solveur Schur p×p par observation** (complément de Schur sur H_MM, en O(np³)). L'approximation diagonale 2×2 était le fallback pour `p > thresh`.

### Analyse du coût — cas barents (n=89, p=30)

Avec `block_newton_thresh = 30` (défaut), barents full activait le solveur exact :
- Coût : 89 × (920 iters) × (30³ / 6) ≈ **2G FLOPs** par EM step
- Temps observé : 3 s (contre 0.9 s pour nlopt)

Passage à `block_newton_thresh = 0` (approx diagonale) :
- Coût : O(n·p) ≈ 90 × fois moins
- Temps observé : **0.68 s**
- Loglik : **identique** (+14 vs nlopt, inchangé)
- Itérations internes : +15% seulement

La précision supplémentaire du solveur exact n'apporte aucun bénéfice observable, pour un coût prohibitif sur des datasets de taille courante.

### Décision : suppression complète

Le paramètre `block_newton_thresh` est **supprimé** de l'ensemble du code (7 fichiers modifiés) :

| Fichier | Modification |
|---------|-------------|
| `R/utils.R` | Suppression de `block_newton_thresh = 0L` dans `config_default_homemade` |
| `R/PLN.R` | Suppression de la ligne `@param` dans la doc |
| `src/utils.h` | Suppression du champ `int block_newton_thresh = 30` dans `NewtonConfig` et du parsing |
| `src/CovarianceTraits.h` | `DenseOmegaImpl::compute_joint_step_MS` et `compute_step_M` : suppression du paramètre et du bloc `if (thresh > 0)` avec les solveurs Schur |
| `src/CovarianceTraits.h` | `DiagonalCovTraits`, `SphericalCovTraits` : suppression du paramètre commenté `/*block_thresh*/ = 0` |
| `src/newton_impl.h` | `newton_optimize_impl` et `newton_vestep_impl` : suppression du paramètre ; correction de virgule résiduelle |
| `src/newton_full_cov.cpp`, `newton_fixed_cov.cpp` | Suppression de `cfg.block_newton_thresh` dans les appels |

---

## 22. Augmentation de `maxit_em` : 50 → 200 (12/06)

**Fichier** : `R/utils.R`

### Problème

Trichoptera full (nocov) et oaks full convergeaient `FALSE` avec `maxit_em = 50`. L'ELBO progressait encore à la 50ème itération externe : il manquait simplement du budget, pas un défaut algorithmique.

### Correction

`maxit_em = 50L → 200L` dans `config_default_homemade`. Résultat : oaks full et trichoptera full (nocov) convergent désormais (`conv = TRUE`).

**État final de `config_default_homemade`** :
```r
config_default_homemade <-
  list(
    algorithm           = "NEWTON",
    backend             = "homemade",
    maxeval             = 10000,
    ftol_in             = 1e-8,
    maxit_em            = 200,
    ftol_em             = 1e-8
  )
```

---

## 23. Benchmark complet — 6 jeux de données (12/06)

**Fichiers** : `inst/benchmark/analyse_convergence.R`, `inst/benchmark/compare_backends.R`, `inst/.gitignore` (via `.gitignore` racine), `.Rbuildignore`

### Jeux de données couverts

| Dataset | n | p | Covariances testées |
|---------|---|---|---------------------|
| trichoptera | 49 | 17 | full, diag, sph |
| barents | 89 | 30 | full, diag, sph |
| mollusk | 163 | 32 | full, diag, sph |
| oaks | 116 | 114 | full, diag, sph |
| microcosm | 880 | 259 | diag, sph, **full** |
| scRNA | 3918 | 500 | diag, sph, **full** |

Toutes les configurations testées avec et sans covariables (12 labels, 3 covariances = 36 paires chacune avec Newton et nlopt).

### Sorties

`inst/benchmark/` (dans `.gitignore` et `.Rbuildignore`) :
- `convergence_trajectory.pdf` — ELBO vs itérations EM
- `convergence_rel_change.pdf` — changement relatif ELBO par itération
- `convergence_step_dist.pdf` — distance des pas (M, S) par itération
- `backend_time.pdf` — temps de calcul Newton vs nlopt
- `backend_loglik.pdf` — différence de loglik (Newton − nlopt)
- `backend_speedup.pdf` — speedup nlopt/Newton par configuration
- `compare_backends_results.csv` — tableau complet

### Résultats clés

**Newton trouve toujours un loglik ≥ nlopt** (invariant robuste sur tous les datasets) :

| Régime | Speedup Newton/nlopt | ll_diff | Conv Newton | Conv nlopt |
|--------|---------------------|---------|-------------|------------|
| **scRNA full** (n=3918, p=500) | **2.1–2.6× plus rapide** | **+867 à +944** | ✓ | ✓ |
| scRNA diag/sph nocov | 1.3–1.4× plus rapide | +7–9 | ✓ | ✓ |
| scRNA diag/sph cov | nlopt légèrement plus rapide | +1–8 | ✓ | ✓ |
| oaks full (n=116, p=114) | ~1× (égal) | +20–21 | ✓ | ✓ |
| **microcosm full** (n=880, p=259) | nlopt 4–5× plus rapide | **+30000** | ✓ | **✗ (conv=FALSE)** |
| barents full | nlopt 1.4–1.8× | +11–14 | ✓ | ✓ |
| mollusk | variable | +0–8 | ✗ | ✗/✓ |

### Cas remarquables

**microcosm full** : nlopt est plus rapide (23 s vs 106 s) mais **ne converge pas** — il s'arrête à un plateau bien inférieur (loglik ≈ −248 000 contre −217 000 pour Newton). Newton est le seul backend fonctionnel sur ce cas.

**scRNA full** : Newton est à la fois plus rapide (+2.1–2.6×) et meilleur en loglik (+867/+944). La grande taille (n=3918) conditionne mieux le problème intérieur et facilite la convergence Newton.

**mollusk** : les deux backends peinent (ELBO décroissant, rel_drop > 100%). Problème d'initialisation loin de l'optimum — hors périmètre de cette session.

**trichoptera diagonal** : nécessite > 200 EM iterations externes — non convergé dans le budget. À investiguer séparément.

---

## Récapitulatif des décisions algorithmiques finales

### Ce qui est fixé (stable)

| Composante | Choix retenu | Raison |
|-----------|-------------|--------|
| Paramétrage S | ψ = log(S²) | Évite les contraintes ≥ 0, meilleur conditionnement |
| Step interne | Pas conjoint Newton 2×2 (M, ψ) | Capture le couplage M-S via H_Mψ, sans coût O(p³) |
| Approximation hessienne | Diagonale par blocs (indépendance i,j) | Exacte pour Schur diagonal, O(np), identique en qualité à l'exact |
| Armijo | Conjoint sur (M, ψ), pente Q_step projetée | Correct avec B profilé (sinon pas trop longs si d/n grand) |
| B profilé | P_X = solve(X'WX, X'W), mis à jour avant chaque step | Élimine le cold-start, enveloppe valide |
| Structure EM | Boucle interne VE (ftol=1e-8) + boucle externe EM (maxit=200, em_tol=1e-8) | Convergence robuste sur grand n |
| Ω, B | M-step analytique après convergence interne | Newton pure uniquement sur (M, ψ) |

### Ce qui est abandonné

| Idée | Pourquoi abandonnée |
|------|---------------------|
| Block Newton (solveur Schur p×p) | O(np³) prohibitif, aucun gain en loglik vs approximation diagonale |
| Point fixe séparé pour S² | Ignore le couplage M-S, convergence plus lente |
| `homemade_alt` distinct de `homemade` | Fusionné dans `homemade` après validation (B profilé + Q_step Armijo) |
| `hybrid` comme défaut | Newton seul suffit sur tous les cas validés |

### Cas limites connus

- **mollusk** : mauvaise initialisation, les deux backends convergent mal
- **trichoptera diagonal** : besoin de > 200 EM iterations (budget à augmenter localement si nécessaire)
- **microcosm full** : Newton 4–5× plus lent que nlopt (mais seul à converger correctement)
- **La comparaison de temps est sensible à la charge BLAS** (multithreadé) : ne pas lancer les scripts en parallèle

---

## 24. ZIPLN — Étape VE conjointe (M, ψ, R) et alignement API (12/06)

**Fichiers** : `src/optim_zi-pln.cpp`, `R/ZIPLNfit-class.R`, `R/ZIPLN.R`, `R/RcppExports.R`, `src/RcppExports.cpp`

### Architecture finale de l'étape VE ZIPLN

Les deux backends gèrent R **en interne** et retournent `(M, S², R)` :

**Backend "homemade" — `ve_step_zipln_newton`**
```
Pour chaque itération :
  R     = σ(A + logit(Pi)) × 1{Y=0}     ← mis à jour à chaque iter depuis (M,S²) courant
  one_m_R = 1 - R
  Hessienne 2×2 par (i,j) :
    h_mm = (1-R)·A + ω_jj
    h_mp = 0.5·S²·(1-R)·A               ← terme croisé (théorème de l'enveloppe)
    h_pp = h_mp·(1 + 0.5·S²) + 0.5·S²·ω_jj
  Pas Newton + Armijo conjoint (M, ψ)
convergence : |f(t)-f(t-1)| < ftol·(1+|f(t-1)|)
```

**Backend "nlopt" — `ve_step_zipln_nlopt`**
```
R₀ = σ(A₀ + logit(Pi)) × 1{Y=0}   ← calculé une fois sur (init_M, init_S²)
R₀ fixé pendant toute la solve nlopt  ← garantit cohérence (objectif, gradient)
Après convergence : R = σ(A_final + logit(Pi)) × 1{Y=0}
Retourne (M, S², R)
```

La fixation de R₀ pendant l'optimisation nlopt est nécessaire : mettre à jour R dans chaque callback de gradient brise la cohérence (objectif, gradient) du point de vue de nlopt, dégradant l'ELBO de ~1000 unités (−36706 vs −33523 sur oaks).

### Alignement API PLN ↔ ZIPLN

`ZIPLN_param()` utilise désormais `make_config_optim()` avec `backend = c("homemade", "nlopt")`, comme `PLN_param()`. Les fonctions renommées :
- `optim_zipln_M_psi`        → `ve_step_zipln_nlopt`
- `optim_zipln_M_psi_newton` → `ve_step_zipln_newton`

### Benchmark (oaks, n=116, p=114, `Abundance ~ 1`)

| Backend | ELBO | Temps |
|---------|------|-------|
| homemade (Newton) | −32 982 | 16.1 s |
| nlopt (CCSAQ)     | −33 517 | 14.2 s |

Newton trouve un ELBO meilleur (+535), à temps comparable.

---

## 25. Corrections doc et code ZIPLN (12/06)

**Fichiers** : `R/ZIPLN.R`, `R/ZIPLNfit-class.R`, `src/optim_zi-pln.cpp`, `tests/testthat/test-zipln.R`

### En-tête `ve_step_zipln_nlopt` (optim_zi-pln.cpp)

Mis à jour pour décrire l'implémentation réelle :
- Précise que la fonction prend Pi (pas R), fixe R₀ avant nlopt, retourne (M, S², R)
- Explication de la fixation R₀ pour la cohérence (objectif, gradient)

### Doc ZIPLN_param (R/ZIPLN.R)

- Titre corrigé : "PLN fit" → "ZIPLN fit"
- `@param backend` ajouté pour surcharger l'héritage de `PLN_param` (qui mentionne "hybrid" et "torch", non supportés par ZIPLN)
- `@details` reformulé pour distinguer entries communes et spécifiques à ZIPLN

### `browser()` supprimé (R/ZIPLNfit-class.R:109)

Résidu de debug dans `initialize()` : `if (max(M[,j]) > 10) browser()`. La condition est rendue impossible par le `pmin(..., 10)` sur la ligne précédente.

### Test NLopt corrigé (tests/testthat/test-zipln.R)

Le test "ZIPLN is working with different optimization algorithm in NLopt" utilisait le backend par défaut ("homemade"), rendant les noms d'algorithme nlopt sans effet. Ajout de `backend = "nlopt"` explicite.

---

## 26. Investigation et suppression de `approx_ZI` (12/06)

**Fichiers** : `R/ZIPLNfit-class.R`, `R/ZIPLN.R`, `R/ZIPLNnetwork.R`, `tests/testthat/test-zipln.R`

### Contexte

L'option `approx_ZI` existait depuis l'ancienne architecture (R séparé du step (M,S)) pour basculer entre deux formes de mise à jour de R :
- `approx_ZI=TRUE` → `optim_zipln_R_var` : R* = σ(A + logit(π)), maximiseur exact de l'ELBO w.r.t. R
- `approx_ZI=FALSE` → `optim_zipln_R_exact` : R = π / (Φ(O+XB, σ²)·(1-π) + π), vraie probabilité bayésienne P(C=1|Y=0) marginalisée sur Z via la fonction W de Lambert

Dans la nouvelle architecture conjointe, `approx_ZI` n'était plus câblé — le step VE utilisait toujours la forme σ, mais le paramètre restait dans le config sans effet.

### Benchmark de comparaison (trichoptera)

| Backend | approx_ZI | ELBO | Itérations |
|---------|-----------|------|-----------|
| Newton  | TRUE  (σ formula) | **−1139.3** | 167 |
| Newton  | FALSE (Lambert W) | −1216.2 | 16 |
| CCSAQ   | TRUE  (σ formula) | **−1140.8** | 64 |
| CCSAQ   | FALSE (Lambert W) | −1226.5 | 27 |

### Analyse

La forme "exacte" bayésienne donne un ELBO inférieur de **77–86 points**. La raison est théoriquement claire : la formule σ(A + logit(π)) est l'**optimum exact de l'ELBO** par rapport à R (∂ELBO/∂R_ij = 0 donne exactement cette formule). La forme Lambert W maximise la vraisemblance marginale vraie, une fonctionnelle différente — convergence plus rapide mais vers un point sous-optimal au sens de l'ELBO.

Le qualificatif "approché" pour la formule σ était trompeur : c'est la forme exacte du point de vue de la borne variationnelle.

### Décision : suppression complète

`approx_ZI` supprimé de :
- `R/ZIPLN.R` : doc + `make_config_optim(extra=...)`
- `R/ZIPLNnetwork.R` : ligne `config_opt$approx_ZI <- TRUE`
- `tests/testthat/test-zipln.R` : test entier supprimé (testait l'équivalence des deux formes, fausse)

Les fonctions C++ `optim_zipln_R_var` et `optim_zipln_R_exact` sont conservées dans `src/optim_zi-pln.cpp` pour mémoire historique.

---

## 27. PLNPCA — Remplacement du gradient spectral par L-BFGS joint + Wolfe (12/06)

**Fichier** : `src/builtin_rank_cov.cpp` (ex `spectral_rank_cov.cpp`)

### Historique des tentatives

| Approche | Résultat |
|----------|---------|
| Gradient spectral BB+GLL | Converge vers un mauvais minimum local sur oaks |
| Armijo monotone (nm_window=1) | Identique — problème structural, pas ligne de recherche |
| EM alterné BB (blocs VE / M) | Même minimum — BB diagonal contaminé par couplage M·Cᵀ |
| L-BFGS alterné par blocs + Armijo | Histoire toujours vide (Armijo ne garantit pas s^T y > 0) |
| **L-BFGS joint + Wolfe fort** | **Succès — meilleur loglik que nlopt sur tous les datasets** |
| L-BFGS par blocs EM + Wolfe interne | Pas mieux que joint, 7–10× plus lent |

### Architecture retenue

Optimisation jointe de tous les paramètres `[vec(B); vec(C); vec(M); vec(ψ)]` avec :
- **L-BFGS** deux-boucles (m=10 paires), initialisation steepest-descent
- **Recherche de Wolfe forte** (Nocedal & Wright Algo 3.5/3.6, c₁=1e-4, c₂=0.9)
  - Garantit s^T y > 0 à chaque pas accepté → historique toujours valide
  - Capture la courbure croisée M·Cᵀ que les méthodes bloc-alternantes manquent
- Warm-start ψ : un pas de point fixe `ψ = −log(1 + A·C²)` avant le premier appel L-BFGS
- Convergence : ftol consécutif + fenêtre glissante de 100 itérations

### Benchmark (séquentiel)

| Dataset / rank | nlopt (CCSAQ) | L-BFGS joint |
|---|---|---|
| trichoptera q=3 | −766.9 / 0.68s | **−763.7** / 0.9s |
| trichoptera q=4 | −700.6 / 0.54s | **−693.6** / 2.5s |
| trichoptera q=5 | −686.2 / 0.70s | **−672.6** / 2.9s |
| oaks q=5 | −97677 / 14.6s | **−78226** / 8.6s† |
| oaks q=10 | −66399 / 15.9s | **−47238** / 17s |

†Résultat de session précédente (joint L-BFGS pur) : −77058 / 8.6s. L-BFGS systématiquement meilleur en loglik, couplage M·Cᵀ bien capturé.

### Pourquoi le bloc-EM L-BFGS est-il moins bon ?

Chaque bloc (VE : M,ψ) et (M-step : B,C) est convexe isolément → L-BFGS converge vite en interne. Mais la convergence EM extérieure est linéaire et annule ce gain : 7–10× plus lent et loglik légèrement pire que joint sur oaks.

---

## 28. Nettoyage et consolidation des fichiers C++ (12/06)

### Consolidation newton_*_cov.cpp

Les quatre fichiers thin-wrapper (full, diagonal, spherical, fixed) sont regroupés en un seul :

| Avant | Après |
|-------|-------|
| `newton_full_cov.cpp` | → |
| `newton_diag_cov.cpp` | → `builtin_newton_pln.cpp` |
| `newton_fixed_cov.cpp` | → |
| `newton_spherical.cpp` | → |

### Suppression du code mort

- `newton_rank_cov.cpp` : coordinate-Newton PLNPCA développé pendant la phase BB/spectral, jamais appelé depuis R après le passage au L-BFGS. Supprimé.

### Renommages de fichiers

| Ancien | Nouveau |
|--------|---------|
| `spectral_rank_cov.cpp` | `builtin_rank_cov.cpp` |
| `newton_impl.h` | `builtin_newton_impl.h` |
| `CovarianceTraits.h` | `builtin_covariance_pln.h` |

### Renommages de fonctions

| Ancien | Nouveau |
|--------|---------|
| `spectral_optimize_rank` | `builtin_optimize_rank` |
| `spectral_optimize_vestep_rank` | `builtin_optimize_vestep_rank` |
| `newton_optimize_*` (7 fonctions) | `builtin_optimize_*` |
| `newton_optimize_impl<T>` | `builtin_optimize_impl<T>` |

### Nettoyage config PLNPCA

- `config_default_spectral` → `config_default_plnpca`
- Champ `algorithm = "LBFGS"` supprimé (jamais lu par le C++, qui ne lit que `maxeval` et `ftol_in`)

---

## 29. Renommage global `"homemade"` → `"builtin"` (12/06)

**Portée** : tous les fichiers R, C++ sources, tests, docs, scripts `inst/`.

Motivation : "homemade" était informel et trompeur. "builtin" décrit mieux qu'il s'agit de l'optimiseur intégré au package, sans dépendance externe.

- Chaînes `"homemade"` dans les valeurs et vecteurs de backend
- Variables `config_default_homemade` → `config_default_builtin`, paramètre `homemade_default` → `builtin_default`
- Commentaires dans les fichiers R et C++
- Documentation `.Rd` régénérée via `devtools::document()`

PLNPCA : `"builtin"` devient le backend **par défaut** (ex `"nlopt"`), car il donne systématiquement un meilleur loglik.

---

## 30. Suppression du backend `"hybrid"` (12/06)

### Benchmark de justification (PLN, séquentiel)

| Dataset | Cov | nlopt | builtin | hybrid |
|---|---|---|---|---|
| trichoptera | full | −1053.0 / 0.68s | **−1051.7** / 0.93s | −1051.7 / 0.88s |
| trichoptera | diagonal | −1109.6 | −1109.7 | **−1109.6** |
| oaks | full | −32048 / 6.5s | **−32028** / 5.9s | **−32028** / 8.2s |
| oaks | diagonal | −38408 | −38408 | −38408 |

### Verdict

`hybrid` (nlopt phase 1 → Newton phase 2) ne dépasse jamais `builtin` seul en loglik et est systématiquement plus lent (il paie le coût nlopt inutilement). Le Newton `builtin` converge déjà bien depuis l'initialisation froide.

### Suppressions

- `make_hybrid_optimizer()` supprimée de `R/utils.R`
- Branche `hybrid` retirée de `setup_optimizer()` dans `R/PLNfit-class.R`
- `"hybrid"` retiré des vecteurs `backend` dans `PLN_param`, `PLNLDA_param`, `PLNmixture_param`, `PLNnetwork_param`
- Documentation `.Rd` régénérée

---

## 31. Correction de l'initialisation de M pour PLNPCA (12/06)

**Fichier** : `R/PLNPCAfit-class.R` (ligne 262)

### Bug

L'initialisation de `private$M` était :
```r
M <- svdM$u[, 1:q] %*% diag(d[1:q]) %*% t(svdM$v[1:q, 1:q])
```

`svdM$v[1:q, 1:q]` prend les premières `q` **lignes** (espèces) de V, pas les premières `q` composantes. C'est une erreur d'index qui donne un résultat numériquement arbitraire.

### Correction

Dans PLNPCA, le modèle est `Z = O + X·B + M·Cᵀ` avec `M` de taille n×q. La contrainte cohérente avec `C = V[:,1:q]·D[1:q]/√n` est que `M·Cᵀ ≈ M_PLN`. En résolvant :

M · (V[:,1:q]·D[1:q]/√n)ᵀ = U·D·Vᵀ  ⟹  M = √n · U[:,1:q]

```r
private$M <- sqrt(self$n) * svdM$u[, 1:rank, drop = FALSE]
```

### Impact mesuré (trichoptera)

| q | Erreur ‖M·Cᵀ − M_PLN‖_F (ancienne) | Erreur ‖M·Cᵀ − M_PLN‖_F (nouvelle) |
|---|---|---|
| 3 | **1174** | **13.6** |
| 5 | **1176** | **8.4** |

### Validation

72 tests `test-plnpcafit.R` passent. Loglik post-convergence inchangés (l'optimiseur L-BFGS corrigeait de toute façon, mais le point de départ est maintenant cohérent).

---

## 32. PLNPCA — Retour à nlopt comme backend par défaut (12/06)

**Fichiers** : `R/PLNPCA.R`, `src/builtin_optim_plnpca.cpp`

### Contexte

Cette section a d'abord été rédigée avec des chiffres issus d'une init SVD bugguée (SVD sur M_PLN complet, sans la correction SVD sur M−XB ni la normalisation X). Ces valeurs (-3988 nlopt barents q=5, -5957 builtin) étaient artefactuelles. Le **benchmark re-effectué avec le code corrigé** (fixes de la section 31 + section 33) donne un tableau très différent.

### Benchmark re-effectué (code-enhancement, code complet avec tous les fixes)

| Dataset | q | nlopt ll | nlopt t | builtin ll | builtin t | gagnant |
|---------|---|----------|---------|------------|-----------|---------|
| trichoptera | 3 | -640.5 | 0.43s | **-638.1** | 0.46s | builtin |
| trichoptera | 5 | -584.2 | 0.44s | **-578.1** | 0.56s | builtin |
| barents     | 3 | -6720.4 | 0.59s | **-6699.9** | 0.80s | builtin |
| barents     | 5 | **-3800.2** | 0.70s | -3836.3 | 0.88s | nlopt |
| barents     | 10 | -3253.6 | 0.79s | **-3080.3** | 1.89s | builtin |
| oaks        | 3 | -111741.1 | 1.15s | **-111715.3** | 1.91s | builtin |
| oaks        | 5 | -77189.8 | 1.29s | **-77178.5** | 2.00s | builtin |
| oaks        | 10 | **-47307.7** | 9.05s* | -47483.8 | 4.37s | nlopt |

*oaks q=10 nlopt : maxeval atteint (10000 iter), solution non convergée.

**Bilan : builtin gagne 6/9 ; nlopt gagne 2/9 (barents q=5, oaks q=10).**

La dichotomie "nlopt toujours meilleur" est donc **fausse** avec le code corrigé. Les deux backends trouvent des bassins locaux différents selon le rang et le jeu de données — sans gagnant universel.

Les analyses intermédiaires (EM alterné, gradient balancing) avaient été conduites sur la base des mauvais chiffres de l'init bugguée et ne sont plus pertinentes.

### Décision

- `PLNPCA_param()` : default maintenu à `"nlopt"` (comportement conservateur, prévisible)
- `"builtin"` reste disponible : souvent meilleur en loglik (6/9 cas), plus rapide sur les petits rangs
- Code C++ `builtin_optim_plnpca.cpp` : joint L-BFGS pur sans EM (plus simple)

### Validation

85 tests (`test-plnpcafamily.R` + `test-plnpcafit.R`) passent.

## 33. PLNPCA — Remplacement de l'init PLN-EM par LM + benchmark init_method (13/06)

**Fichiers** : `R/PLNPCAfamily-class.R`, `R/PLNPCA.R`, `inst/benchmark/compare_plnpca_init.R`

### Motivation

`PLNPCAfamily$initialize` fittait systématiquement un `PLNfit` complet (EM variationnel) comme inception, uniquement pour en extraire B et M et calculer la SVD sur M−XB. Ce coût (0.6–1.8s par jeu de données) est inutile : `compute_PLN_starting_point` (lm.fit) donne directement B_lm et M_lm = log((1+Y)/exp(O)), soit exactement le résidu LM sur lequel la SVD est calculée.

**Argument théorique :** La SVD est calculée sur les résidus de la régression de M sur X. Ces résidus sont orthogonaux à X par construction → re-estimer B pour chaque rang q donne B_q ≈ B_lm. Le B est donc universel en initialisation ; l'optimiseur affine rang par rang.

### Changements

**`R/PLNPCAfamily-class.R`** — `initialize()` :
- Si l'utilisateur fournit un `inception` PLNfit → comportement inchangé (SVD sur M_PLN − XB_PLN)
- Sinon → `compute_PLN_starting_point()` (un `lm.fit`), SVD sur M_lm − XB_lm. Plus de PLN EM.

**`R/PLNPCA.R`** — `PLNPCA_param()` :
- Nouveau paramètre `init_method = c("LM", "GLM")` (cohérence avec `PLN_param`)

### Benchmark init_method × backend (13/06/2026)

Script : `inst/benchmark/compare_plnpca_init.R`  
Conditions : `{LM, GLM, PLN-EM} × {nlopt, builtin}`, jeux trichoptera / barents / oaks.  
"PLN-EM" = ancienne init master (PLNfit complet passé comme inception).

**trichoptera** (q = 1, 3, 5) — toutes méthodes convergent au même endroit, < 10 unités de différence.

**barents** (q = 3, 5, 10, Depth + Temperature) :

| backend | init | q=3 | q=5 | q=10 |
|---------|------|-----|-----|------|
| nlopt | LM | **−6368.5** ✓ | −3714.5 | −3128.5 |
| nlopt | GLM | −6368.1 | −3844.5 | −3321.6 |
| nlopt | PLN-EM | −6393.0 ❌ | **−3642.5** | **−3048.6** |
| builtin | LM | −6368.0 | −3685.0 | −3131.9 |
| builtin | GLM | **−6311.5** | −3681.3 | −3062.1 |
| builtin | PLN-EM | −6415.9 ❌ | −3614.2 | −3047.5 |

Sur q=3, LM bat PLN-EM (~25 unités). Sur q=5,10, PLN-EM aide (~70 unités).

**oaks** (q = 5, 10, 20) :

| backend | init | q=5 | q=10 | q=20 |
|---------|------|-----|------|------|
| nlopt | LM | −77189.8 | **−47307.7** ✓ | −30654.7 |
| nlopt | PLN-EM | −77239.0 | −47477.3 | −30537.6 |
| builtin | LM | −77178.5 | −47483.8 | −30368.8 |
| builtin | PLN-EM | **−77126.7** | −47337.4 | **−30281.9** |

GLM systématiquement le pire sur oaks (initialisation Poisson GLM produit un M résiduel mal aligné).

### Bilan

- **LM est une bonne init par défaut** : aussi bon ou meilleur que PLN-EM sur barents q=3, compétitif partout, sans coût EM
- **PLN-EM aide aux grands rangs** sur datasets avec covariables fortes (barents q=5,10 : +70 ll), mais peut nuire aux petits rangs
- **GLM à éviter pour PLNPCA** : systématiquement pire que LM
- Décision : conserver `init_method = "LM"` comme défaut ; l'utilisateur peut passer `inception = PLN(...)` pour grands rangs si nécessaire

### Validation

85 tests passent.

## 34. Benchmark complet master vs code-enhancement — tous modèles × backends (13/06)

**Script** : `inst/benchmark/run_branch.R`  
**Données** : `inst/benchmark/bench_full_results.rds`  
**Protocole** : master compilé dans `/tmp/PLNmodels-master` (worktree git), CE dans `/tmp/Rlib_ce`. Tout séquentiel. Datasets : trichoptera / barents / oaks.

### Backends disponibles par branche

| Modèle | master | code-enhancement |
|--------|--------|-----------------|
| PLN | nlopt, ~~torch (KO)~~ | **builtin** (Newton), nlopt, torch |
| ZIPLN | nlopt | **builtin**, nlopt |
| PLNPCA | nlopt, ~~torch (KO)~~ | nlopt, **builtin**, torch |
| PLNnetwork | nlopt | nlopt |

### Comparaison loglik : master nlopt vs meilleur backend CE

| Modèle | Dataset | best CE backend | ll master | ll CE | Δll | speedup |
|--------|---------|-----------------|-----------|-------|-----|---------|
| PLN | trichoptera | builtin | −1130.0 | **−1129.6** | +0.5 | 0.7× |
| PLN | barents | builtin | −8520.3 ❌ | **−4402.1** | **+4118** | — |
| PLN | oaks | builtin | −32082.3 | **−32028.5** | +53.9 | **3.0×** |
| ZIPLN | trichoptera | builtin | −1141.1 | **−1139.3** | +1.8 | 0.5× |
| ZIPLN | oaks | builtin | −33346.9 | **−32680.7** | **+666** | 0.7× |
| PLNPCA | trichoptera | builtin | −711.2 | **−694.8** | +16.4 | 0.8× |
| PLNPCA | barents | builtin | −3681.2* | −3685.0 | −3.8 | 0.6× |
| PLNPCA | oaks | builtin | **−77159.5** | −77178.5 | −19.0 | 0.8× |
| PLNnetwork | trichoptera | nlopt | **−1241.9** | −1251.2 | −9.3 | 0.4× |
| PLNnetwork | oaks | nlopt | −38448.8 | **−37603.2** | **+846** | **2.8×** |

*PLNPCA barents master : 10000 itérations atteintes (non convergé).

### Interprétation par modèle

**PLN — barents : +4118 ll (régression majeure corrigée)**  
Master nlopt barents converge en 14 itérations vers ll=−8520 (XTOL prématuré). Même cause que le bug PLNPCA : Depth en mètres → ||g_B||_init énorme → CCSAQ blowup → bassin inférieur. CE résout le problème structurellement : backend builtin (Newton VE + M-step EM) immunisé aux problèmes de scaling X ; nlopt CE également meilleur (−4412 vs −8520) grâce à la refactorisation en EM structuré vs joint CCSAQ master.

**PLN — oaks : +54 ll, 3× plus rapide**  
Newton (builtin) converge en 715 itérations (1.5s) vs 2170 (4.5s) pour nlopt master. Même loglik final (très proche), bien plus efficace.

**ZIPLN — oaks : +666 ll**  
VE step conjoint (M,ψ,R) de CE capture la structure de zero-inflation nettement mieux que la version séquentielle master sur un grand dataset (p=114). Sur trichoptera (petit), la différence est mineure (+1.8).

**PLNPCA — trichoptera : +16 ll**  
Fix SVD résidu (M−XB au lieu de M brut) donne une meilleure init → meilleure convergence.

**PLNPCA — barents/oaks : −3 à −19 ll**  
CE utilise LM init (sans PLN-EM). Sur barents, master avait PLN-EM init ET n'a pas convergé (10000 iter maxeval). Sur oaks, différence de −19 ll avec master — mais master converge en 2464 iter vs CE en 1152 iter builtin.

**PLNnetwork — oaks : +846 ll, 2.8× plus rapide**  
PLNnetwork initialise via PLN. CE's PLN (Newton) donne un meilleur point de départ et est 3× plus rapide → PLNnetwork hérite des gains PLN.

**torch : stable dans CE, cassé dans master**  
PLN torch et PLNPCA torch (barents, oaks) divergent systématiquement dans master (RPROP, lr=0.1). CE les stabilise — probablement via les corrections d'init ou le gradient ELBO.

### Résultats détaillés par backend CE

| Modèle | Dataset | builtin ll | nlopt ll | torch ll | builtin t | nlopt t |
|--------|---------|-----------|---------|---------|-----------|---------|
| PLN | trichoptera | −1129.6 | −1130.7 | −1130.6 | 0.63s | 0.54s |
| PLN | barents | **−4402.1** | −4412.9 | −4566.5 | 1.02s | 0.76s |
| PLN | oaks | **−32028.5** | −32048.5 | −32042.5 | **1.50s** | 2.43s |
| ZIPLN | trichoptera | **−1139.3** | −1140.8 | — | 0.97s | 0.88s |
| ZIPLN | oaks | **−32680.7** | −33351.4 | — | 8.69s | 8.71s |
| PLNPCA | trichoptera | **−694.8** | −695.0 | −703.7 | 0.98s | 0.70s |
| PLNPCA | barents | **−3685.0** | −3714.5 | −3782.1 | 2.86s | 1.45s |
| PLNPCA | oaks | **−77178.5** | −77189.8 | −77355.4 | 8.29s | 5.59s |

---

# Journal de développement — 15 juin 2026

Branche : `code-enhancement`

---

## 35. PLNPCA — `init_method = "EM"` : EM tronqué pour l'initialisation SVD (15/06)

**Fichiers** : `R/PLNPCA.R`, `R/PLNPCAfamily-class.R`

### Motivation

L'init par défaut `"LM"` donne `M₀ = log((1+Y)/exp(O))` — une transformation log naïve, jamais mise à jour par le VE-step. Pour les grands rangs (`rank > sqrt(p)`), l'écart entre M₀ et la vraie moyenne latente se traduit par un mauvais SVD résiduel et une initialisation sous-optimale de C.

L'alternative `inception = PLN(...)` donne la meilleure qualité mais exige la convergence complète du PLN (coûteux). L'idée : **EM tronqué** — K itérations EM seulement, avec le backend builtin (Newton, rapide), pour obtenir une M plus proche de la vraie valeur sans payer le coût complet.

### Implémentation

Ajout de `init_method = "EM"` et `init_niter = 5L` dans `PLNPCA_param()`.

Dans `PLNPCAfamily$initialize`, nouveau branchement :

```r
} else if (identical(control$init_method, "EM")) {
  niter  <- if (is.null(control$init_niter)) 20L else control$init_niter
  em_ctrl <- PLN_param(backend = "builtin", trace = 0,
                       config_optim = list(maxit_em = niter))
  pln_em <- PLNfit$new(responses, covariates, offsets, weights, formula, em_ctrl)
  pln_em$optimize(responses, covariates, offsets, weights, em_ctrl$config_optim)
  init_B <- pln_em$model_par$B
  init_M <- pln_em$var_par$M
```

**Subtilité** : après calcul du SVD, `control$init_method` est forcé à `"LM"` avant la création des `PLNPCAfit` individuels — sinon `PLNfit$initialize` (appelé par `super$initialize`) passerait `"EM"` à `compute_PLN_starting_point` qui ne le reconnaît pas.

### Résultats — oaks (p=114), scan `init_niter`

| init | rank=5 | rank=20 |
|------|--------|---------|
| LM (défaut) | **−77189.8** | −30654.7 |
| EM K=2 | −77444 | **−30525.6** (+129) |
| EM K=5 | −78003 | **−30518.8** (+135) |
| EM K=10 | −78437 | −30547.5 (+107) |
| EM K=20 | −78443 | −30620.5 (+34) |

### Leçon clé — phénomène de sur-convergence

L'EM tronqué aide uniquement pour **`rank > sqrt(p)`**. Pour les petits rangs, il nuit systématiquement (rank=5, p=114 : −800 ll). La cause : après K itérations, la M s'est "spécialisée" pour expliquer l'espace complet à p dimensions. Les K directions dominantes du SVD de cette M sur-convergée sont moins discriminantes pour un rang faible.

Le sweet spot est **K = 2–5** : au-delà, l'effet de sur-convergence domine. Défaut fixé à `init_niter = 5L`.

### API

```r
# grands rangs (rank > sqrt(p)) :
PLNPCA_param(init_method = "EM")              # K=5 par défaut
PLNPCA_param(init_method = "EM", init_niter = 3)

# qualité maximale (coûteux) :
PLNPCA_param(inception = PLN(formula, data))
```

`"LM"` reste le défaut. `"EM"` est un opt-in documenté.

---

## 36. PLNnetwork — `inception_backend` et `inception_niter` : init plus rapide et meilleure (15/06)

**Fichiers** : `R/PLNnetwork.R`, `R/PLNnetworkfamily-class.R`

### Motivation

`PLNnetworkfamily$initialize` lance toujours un PLN complet avant la grille de pénalités. Sur oaks (p=114), ce PLN d'inception prend ~4s (nlopt) sur les ~6.5s totales.

Par ailleurs, `inception_cov = "diagonal"` était déjà supporté mais **cassé** : la Σ diagonale donne des off-diagonaux nuls → `max_pen = 0` → grille de pénalités invalide.

### Deux apports

**1. Correctif `inception_cov != "full"`**

Dans `Networkfamily$initialize`, la Σ pour `max_pen` est désormais calculée comme covariance empirique des résidus latents quand la Σ fittée n'est pas plein-rang :

```r
Sigma_maxpen <- if (!is.null(control$sigma_inception)) {
  control$sigma_inception          # résidus empiriques (M − X·B)ᵀ(M − X·B)/n
} else {
  as.matrix(control$inception$model_par$Sigma)
}
```

`sigma_inception` est calculé dans `PLNnetworkfamily$initialize` quand `inception_cov != "full"` ou `inception_niter` est fixé.

**2. `inception_backend` et `inception_niter`**

Deux nouveaux paramètres dans `PLNnetwork_param()` :

- `inception_backend = NULL` : backend pour le PLN d'inception seulement (le reste de la grille utilise `backend`).
- `inception_niter = NULL` : limite les itérations de l'inception (`maxit_em` pour builtin, `maxeval × 10` pour nlopt).

Implémentation : un `ctrl_inc` séparé est construit via `make_config_optim(inc_backend, ...)` et utilisé pour `PLNfit$new()` **et** `PLNfit$optimize()` (le backend doit être cohérent entre les deux car `setup_optimizer` est appelé à `new()`).

### Résultats — benchmark `inception_backend = "builtin", inception_niter = 5`

| Dataset | nlopt full (défaut) | builtin K=5 | Δ ll | speedup |
|---------|-------------------|-------------|------|---------|
| trichoptera (p=17) | −1149 | −1148 | **+1** | ~1× |
| barents (p=30) | −5226 | −5106 | **+120** | ~1× |
| oaks (p=114) | −37827 | −37435 | **+392** | 1.4× |

Le même phénomène de sur-convergence que PLNPCA s'applique : un inception PLN trop convergé (K=50 ou convergence complète builtin) donne un mauvais warm-start pour les modèles pénalisés. Le sweet spot est K=5–10.

### Leçon clé — M sur-convergée nuit au warm-start pénalisé

Un inception complètement convergé fournit une M optimale pour le problème **non-pénalisé**. Or, les modèles de la grille GLASSO ont besoin d'une M qui s'adapte à des Ω contraints (sparse). Partir d'une M très bien ajustée au PLN plein rend la transition vers les solutions sparse plus difficile. L'EM tronqué laisse M "à mi-chemin" — meilleur point de départ pour la relaxation pénalisée.

Ce principe est général : **l'init d'une collection de modèles plus contraints bénéficie d'une initialisation sous-convergée du modèle père**.

### API

```r
PLNnetwork_param(inception_backend = "builtin", inception_niter = 5)
```

Recommandé pour grands p. Les défauts restent inchangés (pas de breaking change).

---

## 37. PLNnetwork — Warm-start de Ω dans GLASSO : expérience et abandon (15/06)

**Fichiers** : `R/PLNnetworkfit-class.R`, `R/PLNnetworkfamily-class.R` (revertés)

### Hypothèse

`glassoFast` supporte `start = "warm"` avec `w.init` (Σ) et `wi.init` (Ω). L'idée : passer le Ω de l'itération précédente comme point de départ du prochain appel GLASSO.

Deux variantes testées :
1. **Intra-boucle** : warm-start Ω entre les itérations GLASSO→VEM d'un même modèle.
2. **Inter-modèles** : passer `Omega = models[[m]]$model_par$Omega` au modèle m+1 via `update()`.

### Résultats — oaks

| Variante | ll | temps | EM iters |
|----------|-----|-------|---------|
| cold (défaut) | **−37827** | **6.6s** | 90 |
| warm intra-boucle | −37878 | 7.6s | 90 |
| warm inter-modèles | −37838 | 10.9s | 88 |

Toutes les variantes sont pires en qualité **et** en temps.

### Explication

**Intra-boucle** : entre deux appels GLASSO successifs, la Σ change (le VEM met à jour M et B → nouvelle Σ via le M-step). Le Ω optimal sous Σ_{k-1} est loin de l'optimum sous Σ_k. Le cold start (depuis la diagonale de S) est plus proche de la solution sous la nouvelle Σ.

**Inter-modèles** : le path de pénalités est **décroissant** (sparse → dense). Le Ω sparse du modèle m est un mauvais point de départ pour le modèle m+1 moins pénalisé : glassoFast doit "activer" de nouveaux edges depuis une solution trop sparse, ce qui est plus lent que partir de la diagonale. L'algorithme GLASSO warm-start standard fonctionne dans l'autre sens (dense → sparse).

### Conclusion

Le cold start glassoFast (initialisation diagonale interne) est optimal dans les deux contextes du PLNnetwork. **Revert complet.** L'implémentation existante était correcte.

---

## 38. Vérification du mécanisme warm-start (B, M, S2) dans PLNnetwork (15/06)

### Question

Le warm-start séquentiel (B, M, S2) entre modèles consécutifs est-il correctement implémenté et efficace ?

### Audit du code

`PLNnetworkfit$optimize` (boucle while) :
- `args$params` est initialisé une fois avant la boucle (ligne 61), puis **mis à jour ligne 83** à chaque itération avec les valeurs courantes de `private$B/M/S2` — pas de figement.
- `private$Sigma` est mis à jour après chaque VEM step via `do.call(self$update, optim_out)` — `nlopt_optimize_fixed` retourne une Sigma recalculée (M-step : `Σ = (M−XB)ᵀ(M−XB)/n + diag(S²)`).
- Le GLASSO de l'itération suivante utilise donc la Sigma à jour. Le flux GLASSO→VEM→Σ est correct.

`PLNnetworkfamily$optimize` : après convergence du modèle m, le modèle m+1 reçoit (B, M, S2) de m via `update()`.

### Mesure d'efficacité — oaks (30 modèles)

- Total EM outer iterations : **90** (moyenne 3 par modèle)
- Sans warm-start, on attendrait ~10–20 par modèle → économie d'un facteur ~5×

Le warm-start (B, M, S2) est bien implémenté et très efficace. Aucun bug ni amélioration à apporter.

---

## 39. Benchmark backend torch — algorithmes et learning rate (16/06)

**Fichiers** : `R/PLNfit-class.R`

### Objectif

Tester si d'autres choix de `algorithm` ou `lr` améliorent la convergence du backend torch pour PLN (full, diagonal, spherical) sur trichoptera, barents, oaks.

### Résultats benchmark — avant fix

Références builtin : trichoptera=-1051.8, barents=-4410.1, oaks=-32028.5

| dataset    | cov       | algo  | lr   | loglik   | Δ vs builtin |
|------------|-----------|-------|------|----------|-------------|
| trichoptera| full      | RPROP | 0.1  | -1052.1  | -0.3 ✓     |
| trichoptera| diagonal  | RPROP | 0.1  | -1110.0  | -0.3 ✓     |
| trichoptera| spherical | RPROP | 0.1  | -1158.5  | -0.1 ✓     |
| barents    | full      | RPROP | 0.1  | -4557.9  | **-147.8 ✗**|
| barents    | diagonal  | RPROP | 0.1  | -4969.7  | **-260.4 ✗**|
| barents    | spherical | RPROP | 0.1  | -5035.8  | **-258.4 ✗**|
| oaks       | full      | RPROP | 0.1  | -32038.0 | -9.5 ~      |
| oaks       | diagonal  | RPROP | 0.1  | -38410.5 | -2.8 ✓     |
| oaks       | spherical | RPROP | 0.1  | -39450.6 | -0.4 ✓     |

ADAM (toutes config) : ne converge pas en 1000 epochs — uniformément inférieur à RPROP.

**Diagnostic** : barents a des covariables à grande magnitude (Depth ~300). Un pas RPROP de `step_max=50` sur B_Depth génère un changement de 50×300=15000 dans le prédicteur → overflow float32 (max ≈ exp(88)). Trichoptera et oaks n'ont que l'intercept, donc pas de problème.

### Fix : normalisation X en interne dans `torch_optimize`

Même approche que PLNPCA-nlopt (`PLNPCAfit-class.R` lignes 310-311) :

```r
x_scales <- pmax(sqrt(colSums(data$X^2)), 1)
data$X   <- sweep(data$X, 2, x_scales, "/")
params$B <- sweep(params$B, 1, x_scales, "*")
# ... optimisation sur X_sc, B_sc ...
out$B    <- sweep(out$B, 1, x_scales, "/")  # dénormalisation
```

Invariant : `X_sc %*% B_sc = X %*% B` → résultat identique, gradient mieux conditionné.

### Résultats après fix

| dataset    | cov       | lr   | loglik avant | loglik après | Δ après vs builtin |
|------------|-----------|------|-------------|--------------|-------------------|
| barents    | full      | 0.1  | -4557.9     | **-4457.8**  | -47.7            |
| barents    | full      | 0.01 | -4628.3     | **-4452.7**  | -42.6            |
| barents    | diagonal  | 0.1  | -4969.7     | **-4736.7**  | -27.4            |
| barents    | spherical | 0.1  | -5035.8     | **-4810.6**  | -33.2            |
| trichoptera| full      | 0.1  | -1052.1     | -1052.1      | -0.3 (inchangé)  |
| oaks       | full      | 0.1  | -32038.0    | -32040.1     | -11.6 (~inchangé)|

Le fix réduit les gaps de 3–8× sur barents. Torch reste en retrait d'nlopt sur barents (~-40 units vs -12 pour nlopt), mais l'ordre de grandeur est acceptable.

### Conclusions

- **RPROP lr=0.1 reste le meilleur défaut** — aucun changement de `config_default_torch` nécessaire
- **`step_sizes=(1e-3, 50)` conservé** — avec X normalisé, le step max correspond à ~6 unités dans le prédicteur (pour Depth), acceptable
- **ADAM reste déconseillé** — ne converge pas dans le budget 1000 epochs
- **torch est expérimental** mais maintenant utilisable même avec des covariables à grande échelle
- Tests : 302 PASS, 0 FAIL

---

## 40. Uniformisation de la normalisation X pour tous les modèles et backends (16/06)

**Fichiers** : `R/utils.R`, `R/PLNfit-class.R`, `R/PLNnetworkfit-class.R`, `R/ZIPLNfit-class.R`, `R/PLNPCAfit-class.R`

### Motivation

La normalisation des colonnes de X stabilise l'optimisation pour les covariables à grande magnitude (Depth~300). Elle était déjà appliquée dans PLNPCAfit et (après §39) dans `torch_optimize`. Objectif : l'appliquer uniformément à tous les modèles (PLN, PLNPCA, PLNnetwork, ZIPLN) et tous les backends (nlopt, builtin, torch).

### Architecture

**Helper centralisé** dans `utils.R` :
```r
normalize_covariates <- function(X) {
  scales <- pmax(sqrt(colSums(X^2)), 1)
  list(X_sc = sweep(X, 2, scales, "/"), scales = scales)
}
```

**Pattern uniforme** dans chaque `optimize()` :
```r
nrm  <- normalize_covariates(covariates)
# passe X_sc et B_sc = sweep(B, 1, nrm$scales, "*") aux backends
# après optimisation : B <- sweep(B_sc, 1, nrm$scales, "/")
```

**Invariant** : `X_sc %*% B_sc = X %*% B` — le modèle est mathématiquement identique.

### Changements par fichier

| Fichier | Changement |
|---------|------------|
| `R/utils.R` | Ajout de `normalize_covariates()` |
| `R/PLNfit-class.R` | `PLNfit$optimize()` et `PLNfit_fixedcov$optimize()` : normalise X/B, dénormalise B en sortie ; `torch_optimize` : normalisation supprimée (faite en amont) |
| `R/PLNnetworkfit-class.R` | `optimize()` : nrm avant la boucle EM, boucle entière en B_sc (warm-start inclus), dénormalisation après la boucle |
| `R/ZIPLNfit-class.R` | `optimize()` : X local normalisé au début, `parameters$B` initialisé en B_sc, dénormalisé dans `self$update()` |
| `R/PLNPCAfit-class.R` | `optimize()` : code inline remplacé par appel au helper (logique inchangée) |

PLNLDAfit appelle `super$optimize()` → couvert automatiquement, sans modification.

### Résultats numériques

**builtin (Newton 2e ordre) : identique** — Newton H⁻¹g est invariant à l'échelle, résultat mathématiquement exact.

| backend | dataset    | cov      | avant   | après   | Δ      |
|---------|------------|----------|---------|---------|--------|
| nlopt   | trichoptera| full     | -1053.0 | -1053.1 | ~0     |
| nlopt   | barents    | full     | -4422.9 | -4425.6 | -2.7   |
| nlopt   | barents    | diagonal | -4711.0 | -4710.4 | +0.6   |
| nlopt   | barents    | spherical| -4778.5 | -4778.0 | +0.5   |
| nlopt   | oaks       | full     | -32048.5| -32046.7| +1.8   |

La légère régression sur barents full (-2.7 units) est due à CCSAQ qui converge vers un bassin légèrement différent avec X normalisé — pas un bug, mais un comportement d'optimisation non-convexe. Converge bien (1542 iterations, identique avec maxeval=50000). Les autres cas s'améliorent ou restent stables.

**Vérification B** : coefficients Depth restent ~0.01 (échelle originale), pas ~3 (échelle normalisée). Dénormalisation correcte sur tous les modèles.

### Tests : 979 PASS, 0 FAIL, 0 WARN

---

## 41. Migration future/future.apply → parallel/mclapply (16/06)

**Fichiers** : `R/utils.R`, `R/PLNfit-class.R`, `R/PLNPCAfamily-class.R`, `R/PLNnetworkfamily-class.R`, `R/PLNmixturefamily-class.R`, `R/PLNmixture.R`, `R/PLNPCA.R`, `R/PLNmodels-package.R`, `R/PLNPCAfamily-class.R`, `DESCRIPTION`

### Motivation

`future` + `future.apply` interagissent mal avec les bibliothèques BLAS multithreadées (OpenBLAS, MKL) : chaque worker `future::plan("multisession")` spawne un nouveau processus R qui réinitialise BLAS avec N threads → oversubscription (ex. 4 workers × 8 threads BLAS = 32 threads sur 8 cœurs).

`parallel::mclapply` utilise `fork()` — les workers héritent exactement de la config BLAS du parent. L'utilisateur contrôle l'équilibre directement :
```r
Sys.setenv(OPENBLAS_NUM_THREADS = "2")  # 2 threads BLAS par worker
options(mc.cores = 4)                    # 4 workers → 8 cœurs au total
```

### Changements

**10 remplacements** dans 5 fichiers :

| Fichier | Contexte | Avant | Après |
|---------|----------|-------|-------|
| `R/utils.R` | Init GLM (`nullModelPoisson`) | `future_lapply` | `mclapply` |
| `R/PLNfit-class.R` | Jackknife (PLNfit) | `future_lapply` | `mclapply` |
| `R/PLNfit-class.R` | Bootstrap (PLNfit) | `future_lapply` | `mclapply` |
| `R/PLNfit-class.R` | Jackknife (PLNfit_fixedcov) | `future_lapply` | `mclapply` |
| `R/PLNPCAfamily-class.R` | Fitting parallèle des rangs | `future_lapply` | `mclapply` |
| `R/PLNnetworkfamily-class.R` | Stability selection PLNnetwork | `future_lapply` | `mclapply` |
| `R/PLNnetworkfamily-class.R` | Stability selection ZIPLNnetwork | `future_lapply` | `mclapply` |
| `R/PLNmixturefamily-class.R` | `add_one_cluster` | `future_lapply` | `mclapply` |
| `R/PLNmixturefamily-class.R` | `remove_one_cluster` | `future_lapply` | `mclapply` |
| `R/PLNmixturefamily-class.R` | `optimize` (fitting / nb clusters) | `future_lapply` | `mclapply` |

Pattern uniforme : `parallel::mclapply(X, FUN, mc.cores = getOption("mc.cores", 1L))`

**Nettoyage :**
- `DESCRIPTION` : `future` et `future.apply` retirés des `Imports` (`parallel` était déjà listé)
- `R/PLNmodels-package.R` : `@import future` et `@importFrom future.apply ...` supprimés
- `R/PLNPCAfamily-class.R` : `@import future` supprimé
- `R/PLNPCA.R`, `R/PLNmixture.R` : exemples de doc mis à jour (`future::plan(...)` → `options(mc.cores = N)`)

### Usage pour l'utilisateur

```r
# Activer la parallélisation (avant l'appel à PLNPCA, PLNmixture, stability_selection)
options(mc.cores = 4)

# Recommandation avec BLAS multithreadé : équilibrer workers × threads
Sys.setenv(OPENBLAS_NUM_THREADS = "2")  # 4 workers × 2 threads = 8 cœurs

myPCA <- PLNPCA(Abundance ~ ..., data = trichoptera, ranks = 1:10)

# Revenir en séquentiel
options(mc.cores = 1)
```

La valeur par défaut `getOption("mc.cores", 1L)` garantit le comportement séquentiel si l'utilisateur n'a pas positionné `mc.cores` — aucune régression de comportement.

### Limitation

`mclapply` est fork-based et ne fonctionne pas nativement sur Windows (repasse en séquentiel silencieusement). Acceptable pour un package scientifique R principalement utilisé sur Linux/macOS/HPC.

### Tests : 979 PASS, 0 FAIL, 0 WARN

---

## 42. ZIPLN — Remplacement de l'init `pscl::zeroinfl` par LM rapide (16/06)

**Fichiers** : `R/utils.R`, `R/ZIPLNfit-class.R`, `DESCRIPTION`

### Motivation

L'initialisation de ZIPLN parcourait toutes les `p` espèces en boucle et ajustait un modèle ZI Poisson univarié par espèce via `pscl::zeroinfl` (EM complet, 2 GLM par itération). Sur oaks (p=114), cela représentait ~2 s d'init avant même de commencer l'EM ELBO.

Le point de départ n'a pas besoin d'être la solution exacte du modèle ZI Poisson univarié — il suffit d'être dans le bon voisinage pour que l'ELBO converge. De plus, le modèle univarié ignore la structure de covariance jointe, ce qui peut en fait orienter l'init vers un mauvais bassin pour l'ELBO multivarié.

### Nouvelle fonction `compute_ZIPLN_starting_point`

Ajoutée dans `R/utils.R`, exportée publiquement :

```r
compute_ZIPLN_starting_point <- function(Y, X, X0, O, w = NULL) {
  # PLN component: 1 appel lm.fit multivarié (identique à compute_PLN_starting_point "LM")
  sp <- compute_PLN_starting_point(Y, X, O, w, method = "LM")

  # ZI component: taux de zéros empiriques par espèce
  zero_ind <- (Y == 0) * 1.0
  R <- matrix(colMeans(zero_ind), n, p, byrow = TRUE)

  # B0: p GLMs binomiaux sur indicateur de zéro vs X0 (seulement pour ziparam="covar", d0>0)
  B0 <- if (d0 > 0) vapply(1:p, \(j) glm.fit(X0, zero_ind[,j], binomial())$coef, numeric(d0))
        else matrix(0.0, 0L, p)

  list(B = sp$B, M = sp$M, S2 = sp$S2, R = R, B0 = B0)
}
```

### Changements dans `ZIPLNfit$initialize`

La boucle `for (j in 1:p)` avec `pscl::zeroinfl(...)` est remplacée par un unique appel :

```r
sp <- compute_ZIPLN_starting_point(data$Y, data$X, data$X0, data$O, data$w)
private$B <- sp$B; private$B0 <- sp$B0
private$M <- sp$M; private$S2 <- sp$S2; private$R <- sp$R
```

Garde ajoutée pour le cas dégénéré `ncol(X)==0` (préserve l'erreur attendue par les tests).

### Nettoyage

- `@importFrom pscl zeroinfl` et `@importFrom tidyr replace_na` retirés de `ZIPLNfit-class.R`
- `pscl` retiré des `Imports` de `DESCRIPTION` (dépendance lourde, plus nécessaire)

### Gains mesurés

**Temps d'initialisation :**

| Dataset | p | Init LM | Init pscl (extrapolé) | Rapport |
|---------|---|---------|----------------------|---------|
| barents | 30 | ~9 ms | ~570 ms | **×60** |
| oaks | 114 | ~9 ms | ~2170 ms | **×240** |

**Loglik final (backend builtin, zi="single") :**

| Dataset | Nouvelle init LM | Ancienne init pscl (réf) | Δ |
|---------|-----------------|--------------------------|---|
| trichoptera | −1139.9 | −1139.3 | ≈ 0 |
| oaks | **−32117** | −32680 | **+563** |
| barents | −4307 | — | — |

L'init pscl orientait apparemment l'ELBO vers un mauvais bassin sur oaks : le modèle univarié sans structure de covariance jointe produit un B biaisé. L'init LM neutre laisse l'EM converger vers une meilleure solution.

### Tests : 147 PASS (zipln+ziplnnetwork), 205 PASS (plnfit+backends), 449 PASS (plnpca+mixture+se), 0 FAIL

---

## 43. ZIPLNnetwork — Ouverture du backend builtin (16/06)

**Fichiers** : `R/ZIPLNnetwork.R`

### Motivation

ZIPLNnetwork était bloqué à `backend="nlopt"` par un `stopifnot(backend %in% c("nlopt"))`. Comme `ZIPLNfit_sparse` hérite de `ZIPLNfit` sans surcharger `optimize()`, le backend Newton (VE step conjoint M,ψ,R) était architecturalement compatible mais inaccessible.

### Changement

Remplacement du bloc config manuel par `make_config_optim(backend, config_optim, trace, extra = list(ftol_out=1e-6, maxit_out=50))`, parallèle à `PLNnetwork_param()`. Backend "builtin" exposé comme option.

### Résultats benchmark

| Dataset | builtin ll | nlopt ll | Δ (builtin − nlopt) | Temps builtin | Temps nlopt |
|---------|-----------|---------|---------------------|--------------|-------------|
| trichoptera | −1252.8 | −1255.1 | **+2.3** | 2.5 s | 2.1 s |
| oaks | −37843 | −37602 | **−241** | 60 s | 22 s |

### Conclusion : nlopt reste le défaut

Le backend Newton est sur-agressif dans le contexte de la boucle EM alternante GLASSO ↔ VE step : sur oaks (grand dataset), il produit des oscillations et converge 2.7× plus lentement vers une moins bonne solution. Sur trichoptera (petit dataset), il est légèrement meilleur.

**Decision :** `backend = c("nlopt", "builtin")` — nlopt premier = défaut. "builtin" disponible comme option expérimentale pour petits datasets.

Analogue à PLNPCA où nlopt est le défaut malgré que builtin soit souvent meilleur (mais moins stable).

### Tests : 55 PASS (ziplnnetwork), 0 FAIL

---

## 44. ZIPLNnetwork — E-step partiel Newton (`maxit_ve`) (16/06)

**Fichiers** : `R/ZIPLNfit-class.R`, `R/ZIPLNnetwork.R`

### Diagnostic confirmé

Le benchmark de la section 43 montrait que Newton (maxit_em=200) oscillait sous l'alternance GLASSO/VE-step. Hypothèse : le VE step sur-convergé produit un M* très précis pour le Ω courant, mais GLASSO change Ω substantiellement à l'itération suivante → zig-zag.

**Solution : E-step partiel.** Limiter Newton à K itérations par tour d'EM extérieur, sans attendre la convergence complète. Fondement théorique : EM généralisé (Neal & Hinton 1998) — il suffit de *progresser*, pas de converger, à chaque E-step.

### Implémentation

**`ZIPLNfit-class.R`** — priorité dans la lecture du `maxiter` Newton :

```r
maxiter <- as.integer(
  if (!is.null(control$config_optim$maxit_ve))  control$config_optim$maxit_ve
  else if (!is.null(control$config_optim$maxeval)) control$config_optim$maxeval
  else 10000L)
```

`maxit_ve` est lu en priorité sur `maxeval`, sans toucher au comportement standalone (`ZIPLN_param()` ne définit pas `maxit_ve` → `maxeval=10000` comme avant).

**`ZIPLNnetwork.R`** — ajout dans `extra` :

```r
extra = list(ftol_out = 1e-6, maxit_out = 50, maxit_ve = 1L)
```

### Benchmark (oaks et trichoptera)

Sweep `maxit_ve ∈ {1, 5, 10, 50}` vs nlopt sur oaks :

| `maxit_ve` | loglik | temps |
|---|---|---|
| **1** | **−37601.8** | 53.5 s |
| 5 | −37874.1 | 57.8 s |
| 10 | −37817.3 | 66.1 s |
| 50 | −37843.0 | 93.8 s |
| nlopt | −37602.2 | 31.1 s |

`maxit_ve = 1` : qualité identique à nlopt, oscillations disparues. Mais 1.7× plus lent que nlopt sur oaks (chaque pas Newton coûte plus cher que CCSAQ malgré moins d'itérations).

Sur trichoptera :

| Backend | loglik |
|---|---|
| builtin maxit_ve=1 | **−1243.6** |
| builtin maxit_ve=5 (ancien) | −1252.8 |
| nlopt | −1255.1 |

**Défaut final : `nlopt` (stable, plus rapide sur grands datasets). `builtin` avec `maxit_ve=1` disponible comme option — meilleur sur petits datasets (+11.5 ll sur trichoptera), équivalent sur grands (oaks ≈ nlopt).**

### Tests : 55 PASS, 0 FAIL

---

## 45. PLNnetwork — `maxit_ve` : E-step partiel Newton (16/06)

**Fichiers** : `R/PLNnetworkfit-class.R`, `R/PLNnetwork.R`

### Contexte

Même problème que ZIPLNnetwork (section 44) : le backend `builtin` sur-converge le VE step interne avant chaque M-step GLASSO, causant des oscillations. Solution identique : E-step partiel via `maxit_ve`.

### Implémentation

**`PLNnetworkfit-class.R`** — création d'un `inner_config` limitant les itérations internes :

```r
inner_config <- config
if (!is.null(config$maxit_ve)) {
  if (config$backend == "builtin") inner_config$maxit_em <- as.integer(config$maxit_ve)
  else                             inner_config$maxeval  <- as.integer(config$maxit_ve) * 10L
}
args <- list(data=..., params=..., config = inner_config)
```

**`PLNnetwork.R`** — `maxit_ve = NULL` ajouté comme paramètre explicite de `PLNnetwork_param()`. Fusionné dans `config_optim` avant `make_config_optim()`. Défaut `NULL` = comportement inchangé.

### Benchmark complet (trichoptera, barents, oaks)

| Configuration | trichoptera | barents | oaks | temps oaks |
|---|---|---|---|---|
| nlopt (défaut) | −1237.2 | −4618.2 | −37919.6 | 19 s |
| nlopt + incep builtin niter=5 | −1206.4 | −4605.2 | −37418.0 | 14 s |
| builtin maxit_ve=1 | −1238.0 | −4597.1 | −38874.3 ← mauvais | 22 s |
| **builtin maxit_ve=1 + incep builtin niter=5** | **−1203.1** | **−4495.3** | **−37243.9** | **20 s** |

### Résultats

L'inception est **critique** pour `backend="builtin"` : sans elle, builtin seul démarre d'un mauvais bassin sur oaks (−38874 vs nlopt −37920). Avec `inception_backend="builtin"` + `inception_niter=5` :

**Meilleure combinaison sur tous les datasets** :
```r
PLNnetwork_param(backend="builtin", maxit_ve=1,
                 inception_backend="builtin", inception_niter=5)
```

Gains vs nlopt défaut : **+34 ll (trichoptera), +123 ll (barents), +676 ll (oaks)**, pour un temps similaire.

La doc `PLNnetwork_param()` est mise à jour avec cette recommandation et un tableau de benchmark.

### Approche unifiée PLNnetwork / ZIPLNnetwork

Les deux modèles réseau utilisent désormais le même mécanisme `maxit_ve` pour le E-step partiel Newton. Pour ZIPLNnetwork, `maxit_ve=1` est le défaut de `backend="builtin"` (sans inception_backend car le bénéfice est moins critique). Pour PLNnetwork, l'inception builtin tronquée est essentielle.

### Tests : 143 PASS, 0 FAIL

---

# Journal de développement — 16 juin 2026

Branche : `code-enhancement`

---

## 46. ZIPLN — VE step profilée sur les coefficients de régression B (16/06)

**Fichiers** : `src/optim_zi-pln.cpp`

### Motivation

Dans PLN (backend builtin), B est **profilé** à chaque pas Newton : `B = P_X * M` (fermé optimal), le pas Newton est ensuite projeté sur `null(X')` via `Q_step = step_M − X*(P_X*step_M)`. Cela élimine B de l'optimisation variationnelle interne et exploite le théorème de l'enveloppe. 

Pour ZIPLN, B n'apparaît que dans le terme KL : `−0.5 * trace(Ω * (M − XB)' (M − XB))`. L'estimateur fermé de B donné M est identique : `B_opt(M) = (X'X)⁻¹X'M`. La VE step de ZIPLN (Newton joint (M,ψ,R)) peut donc profiler B de la même façon.

### Implémentation (`ve_step_zipln_newton`)

Changements dans `optim_zi-pln.cpp` :

1. **Précalcul de P_X** : `P_X = (X'X)⁻¹X'` (une seule fois par appel VE)
2. **Initialisation de Z** : `Z = O + M_full` (tracké séparément de M_res pour A)
3. **Projection du pas Newton** : `Q_step = step_M − X*(P_X * step_M)` — maintient M_res ∈ null(X')
4. **Armijo cohérent** : Z (pour A) avance par `step_M` (direction complète), M_res par `Q_step` (projeté)
5. **Retour** : `M = Z − O` (M_full avec B profilé implicite)

Par le théorème de l'enveloppe, le gradient de l'ELBO profilé w.r.t. M_full = gradient en coordonnées M_res (∂f/∂B = 0 au point optimal). Le coût additionnel est `O(2dnp)` par itération Newton — négligeable devant `O(np²)` pour Omega.

Validité : puisque le M-step pour B calcule `B = (X'X)⁻¹X'M` avant chaque VE step, on a `M_res_init = (I−H)*M_init ∈ null(X')`. Le projecteur Q_step préserve cette propriété — M_res reste dans null(X') à chaque itération.

### Résultats benchmark (trichoptera / oaks)

| Backend | trichoptera ll | oaks ll | oaks time |
|---------|---------------|---------|-----------|
| nlopt (fixed B) | −1140.8 | −31596.5 | 17.4s |
| **builtin (B-profiling, défaut)** | **−1139.3** | **−31536.7** | **15.2s** |
| Δ (builtin − nlopt) | **+1.5** | **+60** | **−2.2s** |

Le backend `builtin` est déjà le défaut de `ZIPLN_param()` — la B-profilation est donc active par défaut sans aucun changement d'API.

### Tests : 147 PASS, 0 FAIL

---

# Journal de développement — 17 juin 2026

Branche : `code-enhancement`

---

## 47. Uniformisation des signatures VE-step C++ — `(data, params, config)` partout (17/06)

**Fichiers** : `src/nlopt_optim_pln.h`, `src/nlopt_optim_pln.cpp`, `src/builtin_optim_pln.cpp`, `R/PLNfit-class.R`, `src/nlopt_optim_plnpca.cpp`, `src/builtin_optim_plnpca.cpp`, `R/PLNPCAfit-class.R`

### Motivation

Les fonctions VE-step exportées (`nlopt_optimize_vestep_{full,diagonal,spherical}`, `builtin_optimize_vestep_{full,diagonal,spherical}`, et les variantes rang `nlopt_optimize_vestep_rank` / `builtin_optimize_vestep_rank` pour PLNPCA) prenaient `B`/`Omega` (ou `B`/`C`) comme paramètres séparés en plus de `data`/`params`/`config`, alors que toutes les autres fonctions d'optimisation exportées suivent la convention `(data, params, config)` uniquement.

### Implémentation

- `nlopt_vestep_impl<CovTraits>` (template dans `nlopt_optim_pln.h`) et les 3 wrappers `nlopt_optimize_vestep_*` extraient désormais `B`/`Omega` depuis `params["B"]`/`params["Omega"]` au lieu de les recevoir en arguments.
- Idem côté builtin : les 3 wrappers `builtin_optimize_vestep_*` font `Rcpp::as<arma::mat>(params["B"])` / `params["Omega"]`.
- Idem pour PLNPCA : `nlopt_optimize_vestep_rank` et `builtin_optimize_vestep_rank` lisent `B`/`C` depuis `params`.
- Côté R, seul `optimize_vestep()` (méthode privée commune à `PLNfit`/`PLNfit_diagonal`/`PLNfit_spherical`/`PLNLDAfit` via héritage) construisait réellement les arguments d'appel — il injecte maintenant `B`/`Omega` dans `params0` avant l'appel `do.call`. Vérifié par grep qu'aucun autre site R n'invoque ces fonctions avec des arguments construits (les autres occurrences sont des `setup_optimizer(...)` qui ne font que stocker des références de fonction).
- Pour PLNPCA, `PLNPCAfit-class.R::optimize_vestep()` distingue le cas `torch` (signature séparée `B`, `C` conservée, car ce backend n'a pas été touché) du cas nlopt/builtin (B et C injectés dans `params0`).

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 48. Refactorisation CRTP de `covariance_pln.h` : `CovTraitsBase<Derived>` partagé par tous les traits (17/06)

**Fichiers** : `src/covariance_pln.h`, `src/builtin_optim_pln.h`

### Motivation

Audit demandé par l'utilisateur sur les redondances structurelles de `covariance_pln.h` : `vestep_obj_grad`, `profile_and_grad`, `compute_joint_step_MS` et `penalty_M` étaient réimplémentés quasi à l'identique dans `FullCovTraits`/`DiagonalCovTraits`/`SphericalCovTraits` (et hérités via `DenseOmegaImpl` pour Full/Fixed). La seule vraie différence entre `vestep_obj_grad` et `profile_and_grad` est l'appel à `s.update(...)` et la nature du terme de pénalité ajouté (`penalty_M+penalty_S` explicite vs `-elbo_cov`, ce dernier étant une constante quand Omega est profilé en MLE).

### Implémentation

- Nouveau `template<typename Derived> struct CovTraitsBase` fournissant en une seule implémentation : `penalty_M`, `vestep_obj_grad`, `profile_and_grad`, `compute_joint_step_MS`, et un helper privé `vestep_core` (factorise le calcul de `A`, `MO`, `gM`, `gPS`).
- **Point technique clé** : chaque méthode de `CovTraitsBase` est elle-même un *template* sur `State` (déduit à l'appel) plutôt que d'utiliser `typename Derived::State`. À l'endroit où `FullCovTraits` hérite de `CovTraitsBase<FullCovTraits>`, `FullCovTraits` est encore incomplet (CRTP) — toute référence à `Derived::xxx` ne peut donc apparaître que dans un corps de fonction à instanciation différée, jamais dans une déclaration résolue immédiatement par le compilateur.
- Deux nouvelles primitives par trait concret : `diag_scale(S2,s)` (= `S2 ⊙ diag(Omega)` broadcasté) et `add_diag(X,s)` (= `X + diag(Omega)` broadcasté), qui remplacent les `omega_d`/`ones_row` calculés à la main dans chaque `compute_joint_step_MS`. Conséquence : le paramètre `ones_row` (et la construction `arma::ones(n,1)`) disparaît de `builtin_optim_pln.h`, le broadcast Armadillo natif (`each_row()`) suffit.
- `FullCovTraits`/`FixedCovTraits` héritent de `DenseOmegaImpl, CovTraitsBase<Self>` (mixin, pas de diamant) ; `DiagonalCovTraits`/`SphericalCovTraits` héritent juste de `CovTraitsBase<Self>`. Chaque trait concret ne définit plus que `State`, `times_Omega`, `diag_scale`, `add_diag`, `penalty_S`, `mstep`, `elbo_cov`, `output_cov`, `final_loglik`, `has_em`.
- Équivalence mathématique vérifiée terme à terme avant implémentation (pas seulement testée a posteriori) : `penalty_M(MO,M_res,w)+penalty_S(S2,s,w)` ≡ aux formules originales pour les 3 structures de covariance.

### Pourquoi CRTP statique et pas du polymorphisme virtuel (discussion explicite avec l'utilisateur)

1. `has_em` est un `constexpr bool` utilisé dans un `if constexpr` (cf. entrée sur l'audit C++ initial) — perdu avec du virtuel.
2. `State` change de forme selon le trait (`mat`+`vec` / `rowvec` / deux `double`) — une interface virtuelle exigerait un type-erasure (allocation), alors que Spherical n'alloue rien aujourd'hui.
3. Le choix de structure de covariance est fait une fois pour toutes côté Rcpp (une fonction exportée par structure) — jamais de dispatch dynamique nécessaire à l'intérieur d'un appel C++, donc aucun bénéfice à payer le coût d'un vtable dans la boucle chaude (évaluation de gradient, potentiellement des milliers de fois par fit).

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 49. Factorisation de l'objectif Newton (`CovTraitsBase::objective`) + unification convention pré/post-pas (17/06)

**Fichiers** : `src/covariance_pln.h`, `src/builtin_optim_pln.h`

### Motivation

Suite à l'entrée 48, deuxième passe d'audit demandée sur l'exploitation des objets `covariance_pln` dans `builtin_optim_pln.h`/`nlopt_optim_pln.h`. Constat : la formule `accu(w.t()*(A-Y%Z-0.5*logS2)) + Traits::penalty_M(MO,M_res,w) + Traits::penalty_S(S2,state,w)` (valeur d'objectif seule, sans gradient — utilisée par la recherche linéaire Armijo du solveur Newton builtin) était écrite à la main 6 fois dans `builtin_optim_pln.h` (3× dans `builtin_optimize_pln_impl`, 3× dans `builtin_vestep_pln_impl`).

Deuxième constat, plus subtil : les deux fonctions n'enregistraient pas la même convention dans `monitoring$objective`. `builtin_optimize_pln_impl` empilait `f0`, la valeur **avant** le pas de Newton de l'itération courante (valeur de l'itéré précédent). `builtin_vestep_pln_impl` recalculait explicitement la valeur **après** le pas (`obj`, via mise à jour incrémentale `MO_new = MO - alpha*dMO`). Pas un bug de convergence (chaque fonction est cohérente avec elle-même), mais une trace de monitoring non comparable entre `optimize()` et `optimize_vestep()` pour le même backend `newton`.

### Implémentation

- Nouvelle méthode `CovTraitsBase::objective(M_res, Z, S2, logS2, MO, s, Y, w, A)` (template sur `State`), appelée aux 6 emplacements.
- `builtin_optimize_pln_impl` aligné sur la convention post-pas : après le choix de `alpha`, recalcul de `M_res_new = M_res - alpha*Q_step` et `MO_new = MresO - alpha*QstepO` (théorème de l'enveloppe — pas de produit `O(np²)` supplémentaire), puis `obj = Traits::objective(M_res_new, Z, S2, psi, MO_new, ...)` poussé dans `objective_vec`.
- `builtin_vestep_pln_impl` utilisait déjà la convention post-pas — seul l'appel à la formule manuelle est remplacé par `Traits::objective(...)`.

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 50. Portage de `CovTraitsBase`/factorisation nlopt+builtin vers le VE-step ZIPLN (17/06)

**Fichiers** : `src/covariance_zipln.h` (nouveau), `src/builtin_optim_zipln.h` (nouveau), `src/nlopt_optim_zipln.h` (nouveau), `src/wrappers_builtin_optim_zipln.cpp` (nouveau), `src/wrappers_nlopt_optim_zipln.cpp` (nouveau), `src/optim_zi-pln.cpp`, `R/utils-zipln.R`, `R/ZIPLNfit-class.R`

### Motivation

ZIPLN partage les 4 mêmes structures de covariance que PLN (full/diagonal/spherical/fixed), mais son VE-step (`ve_step_zipln_newton`/`ve_step_zipln_nlopt`, dans `optim_zi-pln.cpp`) était un unique couple de fonctions hand-rolled qui traitait Omega comme une matrice dense p×p dans tous les cas — y compris pour diagonal/spherical, où `M_mu * Omega` coûte `O(np²)` au lieu de `O(np)`. Demande explicite de l'utilisateur : portage complet (les deux backends, les 4 structures), en réutilisant la machinerie `CovTraitsBase` de `covariance_pln.h` plutôt que de dupliquer un second jeu de formules.

### Équivalence algébrique exploitée

ZIPLN n'a pas de poids par observation (`w` n'existe pas dans ses données) et le terme de données est barré par `(1-R)` (R = proba ZI variationnelle, nulle là où Y > 0 par construction). Vérifié terme à terme : en posant `A_eff = (1-R) ⊙ A` et `w = ones(n)`, `CovTraitsBase::objective` et `CovTraitsBase::compute_joint_step_MS` (déjà utilisées par PLN) reproduisent **exactement** les formules ZIPLN — y compris le cas en apparence contradictoire où l'ELBO ZIPLN pondère à la fois `Y⊙Z` et `A` par `(1-R)` alors que `CovTraitsBase::objective` ne pondère que `A` : les deux coïncident car `R=0` partout où `Y≠0`.

### Implémentation

- `covariance_zipln.h` : `zipln_update_R(A, logit_Pi, Y_zero)` (formule R* partagée) + `zipln_vestep_obj_grad<Traits>(...)` (gradient nlopt, miroir du `vestep_core` privé de `CovTraitsBase` mais avec `A_eff` fourni par l'appelant au lieu d'être recalculé en interne, puisque le facteur `(1-R)` n'appartient pas à l'abstraction `Traits`).
- `builtin_optim_zipln.h` : `ve_step_zipln_newton_impl<Traits>` — structure identique à la boucle interne de `builtin_optimize_pln_impl` (même profilage de B par projection sur `null(X')`, même pas de Newton joint via `CovTraitsBase::compute_joint_step_MS`), avec en plus la mise à jour de R à chaque itération externe (gelée pendant le pas Newton + line search) et la substitution `A → A_eff` partout.
- `nlopt_optim_zipln.h` : `ve_step_zipln_nlopt_impl<Traits>` — R gelé sur tout le solve nlopt (comme l'original), gradient via `zipln_vestep_obj_grad<Traits>`.
- 8 wrappers exportés (`ve_step_zipln_{newton,nlopt}_{full,diagonal,spherical,fixed}`) dans 2 nouveaux fichiers, construisant le `Traits::State` à partir d'Omega puis déléguant aux templates ci-dessus. Les anciennes fonctions génériques `ve_step_zipln_newton`/`ve_step_zipln_nlopt` sont supprimées d'`optim_zi-pln.cpp` (aucune référence directe dans les tests).
- `R/utils-zipln.R` : nouveau helper `zipln_MS_optimizer(control, suffix)` — factorise le dispatch backend (builtin/nlopt) × structure de covariance vers le bon export C++, utilisé par `ZIPLNfit$initialize` (`"full"`) et les surcharges dans `ZIPLNfit_diagonal`/`_spherical`/`_fixed` (`ZIPLNfit_sparse` reste sur `"full"`, le glasso produisant une matrice Omega dense même si creuse).

### Validation

- Comparaison master vs branche sur trichoptera, 4 structures × 2 backends (8 combinaisons) : backend builtin → loglik **identique au bit** (`-1053.4283`, `-1111.7817`, `-1158.5411`, `-1160.0697`) confirmant le portage Newton exact ; backend nlopt → loglik très proche mais pas bit-exact (`-1055.42` vs `-1055.14` pour full, écarts similaires ailleurs), attendu car CCSAQ est sensible à l'ordre des opérations flottantes dans le lambda objectif/gradient (déjà observé pour d'autres refactos nlopt de cette session, cf. §39).

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 51. ZIPLN — alignement des conventions d'I/O C++ (`PlnData`, `(data,params,config)`, `make_zipln_vestep_result`) (17/06)

**Fichiers** : `src/utils.h`, `src/builtin_optim_zipln.h`, `src/nlopt_optim_zipln.h`, `src/wrappers_{builtin,nlopt}_optim_zipln.cpp`, `R/utils-zipln.R`, `R/ZIPLNfit-class.R`

### Motivation

Question de l'utilisateur après l'entrée 50 : le portage venait de réutiliser `CovTraitsBase` pour les *formules*, mais le code autour (signatures exportées, extraction des données, forme du retour) restait dans le style ad hoc préexistant de ZIPLN — pas `PlnData` (struct Y/X/O/w de `utils.h`), pas `make_vestep_result`/équivalent, pas la convention `(data, params, config)` adoptée pour PLN/PLNPCA à l'entrée 47. Demande explicite : aligner aussi ce niveau-là.

### Implémentation

- `utils.h` : nouveau `make_zipln_vestep_result(M, S2, R, B, status, backend, objective_vec, iterations)`, miroir de `make_vestep_result` mais avec `R` à la place de `Ji` (la log-vraisemblance ZIPLN est recalculée côté R via `zipln_vloglik`, pas dans le VE-step) et `B` en plus (reprofilé depuis le M convergé — champ informationnel, jamais consommé côté R, conservé pour ne pas changer le contrat).
- `nlopt_optim_zipln.h::ve_step_zipln_nlopt_impl<Traits>` prend désormais `(const Rcpp::List & data, const Rcpp::List & params, const Rcpp::List & config)` et fait toute l'extraction lui-même (`PlnData d(data)`, `params["M"|"S2"|"Pi"|"B"|"Omega"]`, `State` construit depuis `Omega`) — miroir exact de `nlopt_vestep_impl` (PLN). Les 4 wrappers nlopt deviennent des one-liners (`return ve_step_zipln_nlopt_impl<FullCovTraits>(data, params, config);`).
- `builtin_optim_zipln.h::ve_step_zipln_newton_impl<Traits>` prend `(const PlnData & d, init_M, init_S2, Pi, B, state, maxiter, ftol_rel)` — miroir de `builtin_vestep_pln_impl`, où l'extraction de `params`/`NewtonConfig` reste dans le wrapper (asymétrie déjà présente entre les deux backends côté PLN, pas une invention de cette entrée). `arma::ones(n)` remplacé par `d.w` (la donnée est désormais threadée plutôt que reconstruite, sans changement de valeur).
- `objective_vec` (jusqu'ici calculé puis jeté côté nlopt, jamais rempli côté builtin) est maintenant effectivement renvoyé dans `monitoring$objective` des deux backends.
- Côté R : `zipln_MS_optimizer()` remplacé par `zipln_MS_fn(backend, suffix)` (simple sélecteur, sans logique de config) + nouvelle méthode privée `ZIPLNfit$setup_MS_optimizer(backend, suffix)` — miroir direct de `PLNfit$setup_optimizer`. `ZIPLNfit::optimize()` et `::optimize_vestep()` construisent désormais `data`/`params`/`config` et appellent `do.call(private$optimizer$MS, list(data=, params=, config=))`, comme `PLNfit::optimize()`/`::optimize_vestep()`.
- Le knob `maxit_ve` (utilisé par `ZIPLNnetwork` pour l'alternance GLASSO partielle, cf. entrée 44) n'existait auparavant que pour le backend builtin (le code nlopt l'ignorait silencieusement — vraisemblablement un oubli, pas un choix). Comportement **préservé à l'identique** : la traduction `maxit_ve → config$maxeval` reste appliquée seulement quand `backend == "builtin"`, maintenant faite dans `optimize()`/`optimize_vestep()` (R) avant le `do.call`, exactement comme `PLNnetworkfit-class.R::optimize()` le fait pour son `inner_config`.
- ZIPLN n'a pas de poids par observation au niveau utilisateur (`data$w` existe bien dans la liste extraite par `extract_model_zi()` mais n'est utilisé nulle part, ni avant ni après cette entrée — latent, hors-scope) ; `w = rep(1, n)` est construit explicitement côté R pour satisfaire `PlnData`, sans réutiliser le `data$w` existant (qui resterait silencieusement ignoré ailleurs — le threader maintenant aurait rendu les poids *partiellement* actifs, uniquement dans le VE-step, ce qui aurait été pire qu'une absence totale de support).

### Validation

- 8 combinaisons (4 structures × 2 backends) sur trichoptera : loglik **identiques** aux valeurs de l'entrée 50 (donc identiques à master pour builtin, et à la version pré-alignement pour nlopt) — confirme que ce changement est une pure restructuration d'I/O, sans aucun effet numérique.
- `ZIPLNnetwork` (alternance GLASSO, `maxit_ve = 1L`, backend builtin) testé séparément sur trichoptera : converge normalement, BIC/loglik cohérents par penalité croissante.
- Suite complète : 979 PASS, 0 FAIL (identique à l'entrée 50).

---

## 52. ZIPLN — nettoyage post-portage : code mort, redondances, calcul inutile (17/06)

**Fichiers** : `src/optim_zi-pln.cpp`, `src/utils.h`, `src/covariance_zipln.h`, `src/builtin_optim_zipln.h`, `src/nlopt_optim_zipln.h`, `src/wrappers_builtin_optim_zipln.cpp`

### Motivation

Relecture demandée par l'utilisateur de toute la partie C++ ZIPLN une fois les entrées 50-51 posées, pour traquer le code mort/redondances/optimisations simples restants.

### Constats et corrections

1. **Code mort** : `optim_zipln_M` et `optim_zipln_psi` (`optim_zi-pln.cpp`) — optimiseurs nlopt mono-variable (M seul, ψ seul) laissés par l'introduction du VE-step joint (M,ψ,R), sans plus aucun appelant R (vérifié par grep sur `R/`, `tests/`, `NAMESPACE`, `man/`). Supprimés. Le commentaire d'en-tête du fichier ("All optimizers use the reparameterization ψ=log(S²)...") ne décrivait plus que ce code mort — supprimé avec.
2. **Redondance** : les 4 wrappers builtin répétaient à l'identique le bloc d'extraction (`PlnData`, `M`, `S2`, `Pi`, `B`, `Omega`, `NewtonConfig`). Factorisé en un template `ve_step_zipln_newton_wrapper<Traits>` (`builtin_optim_zipln.h`) ; les 4 exports de `wrappers_builtin_optim_zipln.cpp` sont désormais des one-liners, au même niveau de facilité que les wrappers nlopt.
3. **Redondance mineure** : `logit(Pi)` et `Y < 0.5` (le masque "R ne peut être non-nul que là où Y=0") étaient recalculés indépendamment dans `builtin_optim_zipln.h` et `nlopt_optim_zipln.h`. Regroupés dans un petit struct `ZiplnRContext` (`covariance_zipln.h`), construit une fois par appel et passé à `zipln_update_R(A, ctx)`.
4. **Calcul payé pour rien** : le champ `B` retourné par le VE-step (recalculé par `solve()` côté nlopt, par `P_X * M` côté builtin) n'était lu par aucun site d'appel R — confirmé par grep sur `ZIPLNfit-class.R` (seuls `$M`, `$S2`, `$R` sont consommés). Supprimé de `make_zipln_vestep_result` (`utils.h`) et des deux impl ; `B` reste un paramètre d'entrée (toujours nécessaire pour initialiser `M_res`/`XB`), seul le calcul de sortie disparaît.

### Validation

- 8 combinaisons (4 structures × 2 backends) sur trichoptera : loglik et nombre d'itérations **identiques au chiffre près** à l'entrée 51 — confirme l'absence d'effet numérique de ce nettoyage.
- Suite complète : 979 PASS, 0 FAIL.

---

## 53. PLN full covariance — exposition de `nlopt_optimize_full_profiled` via `config_optim$profiled` (17/06)

**Fichiers** : `R/utils.R`, `R/PLN.R`, `R/PLNfit-class.R`, `src/wrappers_nlopt_optim_pln.cpp`

### Motivation

Question de l'utilisateur : backend par défaut pour PLN, et comment choisir entre `nlopt_optimize_full` (boucle EM externe, Omega figé pendant chaque solve nlopt interne, B profilé en forme close) et `nlopt_optimize_full_profiled` (B et Omega profilés à chaque éval nlopt, pas de boucle EM) pour la covariance full. Constat : backend par défaut = `"builtin"` (premier élément de `PLN_param(backend = c("builtin","nlopt","torch"))`). `nlopt_optimize_full_profiled` existait déjà comme export C++ mais n'était câblée nulle part côté R — code mort côté API, accessible seulement via `:::`.

### Implémentation

- Nouvelle entrée `profiled = FALSE` dans `config_default_nlopt` (`utils.R`) — partagée par tous les modèles nlopt, mais seulement interprétée pour PLN/covariance full.
- `PLNfit-class.R::initialize` (classe `PLNfit` = covariance full) : `nlopt_main_fn <- if (isTRUE(control$config_optim$profiled)) nlopt_optimize_full_profiled else nlopt_optimize_full`, passé à `setup_optimizer`. Sélectionnable via `PLN_param(backend="nlopt", config_optim=list(profiled=TRUE))`.
- Documentation ajoutée dans `PLN_param()`/`PLNLDA_param()`/etc. (bullet partagée, propagée aux 5 `man/*_param.Rd` via `devtools::document()`).

### Benchmark — résultat surprenant

Hypothèse initiale du commentaire C++ préexistant ("profiled recommandé seulement pour petit p, EM préférable pour grand p" — jamais vérifiée empiriquement, juste déduite du coût `O(np²+p³)` par éval). Benchmark sur données synthétiques (`create_parameters`+`rPLN`, Σ Toeplitz ρ=0.3), n ∈ {50,100,300}, p ∈ {10,...,600}, et sur `oaks` (n=116,p=114) :

| n   | p   | t_EM   | t_profiled | speedup | ll_EM      | ll_profiled |
|-----|-----|--------|------------|---------|------------|-------------|
| 50  | 150 | 1.076s | 0.242s     | 4.45×   | -26686.28  | -26663.10   |
| 100 | 150 | 1.000s | 0.346s     | 2.89×   | -61976.24  | -61956.07   |
| 300 | 600 | 25.52s | 6.20s      | 4.12×   | -520053.03 | -519958.92  |
| oaks (116,114) | — | 2.249s | 1.218s | 1.85× | -32048.18 | -32029.98 |

**`profiled` est systématiquement plus rapide ET atteint un meilleur loglik, sur toute la grille testée** (aucun cas où EM gagne sur les deux critères). Le commentaire C++ existant ("for large p, prefer EM") était donc faux — corrigé en conséquence, avec la nuance que c'est une conclusion empirique (cette grille de tailles/structure de Σ), pas une preuve théorique.

### Décision

Default conservé à `FALSE` (rétrocompatibilité — pas de changement de comportement par défaut sans décision explicite de l'utilisateur), mais `TRUE` semble être le meilleur choix sur la base de ce benchmark. Servira de point de départ si l'utilisateur décide de changer le défaut.

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 54. PLN full covariance — `profiled` passé à `TRUE` par défaut sur cette branche (provisoire) (17/06)

**Fichiers** : `R/utils.R`, `R/PLN.R`, `R/PLNfit-class.R`, `src/wrappers_nlopt_optim_pln.cpp`

Suite à l'entrée 53, l'utilisateur décide de basculer le défaut : `config_default_nlopt$profiled` passe de `FALSE` à `TRUE`. Documentation (`PLN_param()`, `man/*_param.Rd` régénérés) et commentaires (`PLNfit-class.R`, `wrappers_nlopt_optim_pln.cpp`) mis à jour en conséquence.

**Vérification complémentaire demandée par l'utilisateur** : l'hypothèse qu'il avance pour expliquer le gain de l'entrée 53 — `profiled` réduit fortement le nombre d'évaluations nlopt nécessaires (compense le coût `O(np²+p³)` plus élevé par éval) — est confirmée par les compteurs `$optim_par$iterations` :

| n   | p   | iters EM | iters profiled | ratio |
|-----|-----|----------|-----------------|-------|
| 100 | 60  | 166      | 80               | 2.1×  |
| 300 | 150 | 254      | 97               | 2.6×  |
| 100 | 300 | 1594     | 244              | 6.5×  |

**Important — statut provisoire** : l'utilisateur souligne explicitement que ce changement de défaut doit encore être benchmarké proprement (la grille de l'entrée 53 est synthétique/limitée) avant d'être considéré comme définitif. Pour l'instant le défaut `TRUE` ne vit que sur cette branche de développement (`code-enhancement`), pas une décision arrêtée pour une release.

### Tests : 979 PASS, 0 FAIL (suite complète)

---

## 55. ZIPLN — renommage des fonctions C++ exportées et du fichier `optim_zi-pln.cpp` (17/06)

**Fichiers** : `src/optim_zi-pln.cpp` → `src/optim_zipln_mstep.cpp`, `src/builtin_optim_zipln.h`, `src/nlopt_optim_zipln.h`, `src/wrappers_builtin_optim_zipln.cpp`, `src/wrappers_nlopt_optim_zipln.cpp`, `R/utils-zipln.R`

### Motivation

Question de l'utilisateur sur la cohérence des noms de fichiers/fonctions C++. Deux constats :
1. PLN et PLNPCA nomment leurs exports `{backend}_optimize_{type}` / `{backend}_optimize_vestep_{type}` (backend en premier, jamais le nom du modèle dans le nom de fonction — le fichier suffit). ZIPLN gardait la racine historique `ve_step_zipln_{backend}_{type}` (ordre inversé : `ve_step` puis `zipln` puis backend). `zipln` doit rester dans le nom (sinon collision avec les exports PLN, plats côté Rcpp), mais l'ordre des mots peut suivre la convention PLN.
2. `optim_zi-pln.cpp` est le seul fichier ZIPLN à écrire "zi-pln" avec un tiret ; tous les autres (créés/renommés lors du portage CRTP, entrées 50-52) écrivent "zipln" sans tiret. Son contenu (M-step Omega/B + paramètre ZI + `zipln_vloglik`) ne contient plus de VE-step depuis l'entrée 50 — `optim_zipln_mstep.cpp` est plus exact que `optim_zipln.cpp`.

### Implémentation

- Renommage des 8 exports : `ve_step_zipln_newton_{full,diagonal,spherical,fixed}` → `builtin_optimize_vestep_zipln_{full,diagonal,spherical,fixed}` ; `ve_step_zipln_nlopt_{full,diagonal,spherical,fixed}` → `nlopt_optimize_vestep_zipln_{full,diagonal,spherical,fixed}`.
- Templates internes alignés sur la convention PLN (`builtin_vestep_pln_impl`/`nlopt_vestep_impl`) : `ve_step_zipln_newton_impl` → `builtin_vestep_zipln_impl`, `ve_step_zipln_nlopt_impl` → `nlopt_vestep_zipln_impl`, `ve_step_zipln_newton_wrapper` → `builtin_optimize_vestep_zipln_wrapper`.
- `git mv src/optim_zi-pln.cpp src/optim_zipln_mstep.cpp`.
- `R/utils-zipln.R::zipln_MS_fn` mis à jour pour référencer les nouveaux noms.
- `zipln_update_R`/`zipln_vestep_obj_grad` (covariance_zipln.h) inchangés : déjà cohérents avec le préfixe "modèle d'abord" utilisé par PLNPCA (`rank_obj_grad`, etc.).

### Validation

- 8 combinaisons (4 structures × 2 backends) sur trichoptera : loglik identiques au chiffre près (pur renommage, aucun effet numérique attendu ni observé).
- Suite complète : 979 PASS, 0 FAIL.

---

## 56. ZIPLN M-step Omega — réutilisation des `State::update` de `covariance_pln.h` (17/06)

**Fichiers** : `src/optim_zipln_mstep.cpp`

### Motivation

Nouvel audit demandé par l'utilisateur sur les fichiers C++ ZIPLN une fois le renommage fait (simplifications structurelles / optimisations simples). Constat : `optim_zipln_Omega_full`/`_spherical`/`_diagonal` réimplémentaient à la main des formules de M-step déjà présentes dans `covariance_pln.h` (`FullCovTraits::State::update`, `DiagonalCovTraits::State::update`, `SphericalCovTraits::State::update`) — vérifié terme à terme identiques avec `w=ones(n)`, `w_bar=n`. La formule du M-step vivait donc à deux endroits.

### Implémentation

Les 3 fonctions construisent désormais le `State` correspondant (son constructeur 4-arguments appelle `update()`) et en extraient la représentation dense :
- `optim_zipln_Omega_full` : `FullCovTraits::State state(M-X*B, S2, ones(n), n); return state.Omega;` (Omega déjà dense, champ public de `DenseOmegaImpl::State`).
- `optim_zipln_Omega_spherical`/`_diagonal` : `State` stocke `omega2` en `double`/`rowvec` (pas de p×p matérialisée, pour rester cohérent avec le choix de ne jamais allouer une matrice dense dans la boucle chaude PLN) — converti en `arma::diagmat(...)` à la sortie, seul endroit où ça coûte une allocation p×p (une fois par tour EM externe, négligeable).

### Validation

- Comparaison directe ancienne formule (R) vs nouvelle fonction C++ sur données aléatoires (n=30,p=8,d=2) : écarts `< 4e-16` (bruit flottant), confirmant l'équivalence exacte.
- 8 combinaisons (4 structures × 2 backends) sur trichoptera : loglik **builtin identiques au chiffre près** (Newton converge au même point fixe malgré le bruit `1e-16`) ; loglik **nlopt dérive légèrement** (4ᵉ décimale, ex. full : -1055.4227 → -1055.2413) — cohérent avec la sensibilité de CCSAQ à l'ordre des opérations flottantes déjà observée plusieurs fois cette session (cf. §39, §50), pas un signe de régression.
- Suite complète : 979 PASS, 0 FAIL.

---

## 57. Nouveau 5ᵉ trait `GeneticCovTraits` (covariance heritability) — C++ uniquement (17/06)

**Fichiers** : `src/covariance_pln.h`, `src/wrappers_builtin_optim_pln.cpp`, `src/wrappers_nlopt_optim_pln.cpp`

### Contexte

L'utilisateur a fourni `VEM-PLN-struct_var.tex`, décrivant un modèle de covariance $\Sigma=\sigma^2(\rho C+(1-\rho)I_p)$ ($C$ matrice de corrélation fixe fournie par l'utilisateur, ex. matrice de parenté génétique) et son ELBO/gradients. Une implémentation préexistante (`src/optim_genet_cov.cpp::nlopt_optimize_genetic_modeling`) optimise *conjointement* $(\Theta,M,S,\rho)$ via un unique appel nlopt, $\sigma^2$ profilé en forme close — mais **n'est câblée à rien côté R** (pas de `covariance="genetic"`, pas de classe, pas de test : code mort au niveau package, hérité d'une branche `heritability` fusionnée historiquement).

### Vérification des maths du document

Toutes les formules de la section heritability re-dérivées à la main et confirmées correctes ($\log|\Sigma|$, forme matricielle de $J$, $\hat\sigma^2$, gradients $\partial J/\partial\{B,M,S,\rho\}$ — y compris la vérification de signe en tenant compte du fait que `optim_genet_cov.cpp` minimise $-J$). Deux coquilles repérées dans des sections **non utilisées** par le modèle genetic (homoscedastic à $s_i$ scalaire : facteur $s_i$ manquant ; diagonal : $d$ au lieu de $d^{-1}=\omega$ dans l'objectif, le gradient donné juste après étant lui correct) — sans impact, ces sections ne correspondent à aucun code existant.

### Intégration dans le CRTP

Découverte clé : `nlopt_optimize_em_impl<CovTraits>` et `builtin_optimize_pln_impl<Traits>` (déjà utilisés par Full/Fixed) sont des moteurs EM **génériques** — `GeneticCovTraits` s'y branche sans aucune modification de ces moteurs, à condition de fournir `State::update()` (le M-step). Comme Omega reste dense, `GeneticCovTraits : DenseOmegaImpl, CovTraitsBase<GeneticCovTraits>` hérite gratuitement de `times_Omega`/`diag_scale`/`add_diag`/`penalty_S`/`final_loglik` — **le seul code réellement nouveau est `State::update()`** :
- $\sigma^2$ : forme close connaissant $\rho$ (comme avant, théorème de l'enveloppe).
- $\rho$ : pas de forme close → résolu par **bissection 1D** sur $\partial J/\partial\rho$ profilé (formule de la doc), robuste (retombe sur la borne si le gradient ne change jamais de signe sur $(0,1)$).
- $(V,\Lambda)$ (eigendécomposition de $C$) : calculée **une seule fois** par un constructeur factory `State::from_correlation(C)` séparé du constructeur `State(Omega)` (VE-step) — `update()` ne la recalcule jamais, coût $O(p^3)$ payé une fois par itération EM (même ordre que `inv_sympd` pour Full), pas à chaque éval nlopt.
- `elbo_cov`/`output_cov` réécrits pour exploiter $(\sigma^2,\rho,\Lambda)$ directement ($O(p)$) plutôt qu'un `log_det(Omega)` générique ($O(p^3)$) — optimisation immédiate, dans l'esprit des passes précédentes.

Exports ajoutés (mêmes conventions que Full/Fixed) : `{builtin,nlopt}_optimize_genetic`, `{builtin,nlopt}_optimize_vestep_genetic`. L'ancienne fonction monolithique reste en place, intacte, dans `optim_genet_cov.cpp` (gardée pour comparaison, décision de la supprimer reportée à plus tard).

### Bug trouvé et corrigé pendant la validation

Premier jet : `dJ_drho` codé avec le signe de $-\partial J/\partial\rho$ (recopié par erreur depuis la convention de l'ancien code, qui minimise $-J$, alors que ma bissection cherche la racine de $+\partial J/\partial\rho$). Symptôme : $\rho$ convergeait systématiquement vers la borne basse `1e-8` quel que soit le jeu de données. Détecté par dérivée numérique en R du profil $J(\rho)$ comparée à ma formule analytique (signe exactement opposé) — corrigé en une ligne.

### Validation post-correction

- nlopt et builtin convergent vers le **même** $(\rho,\sigma^2)$ et le **même loglik** au chiffre près sur données simulées (n=1000, p=15) — confirme la cohérence des deux backends.
- Balayage indépendant en R du profil $J(\rho)$ au point convergé : l'argmax de la grille coïncide avec le $\rho$ trouvé par bissection.
- `final_loglik` (hérité de `DenseOmegaImpl`, jamais réécrit pour Genetic) vérifié contre un calcul de référence indépendant en R : écart `< 0.003` sur un loglik d'ordre -27000 (bruit numérique).
- L'ancienne implémentation monolithique, testée sur le même jeu de données avec plusieurs `rho` initiaux, reste bloquée près de l'init dans la plupart des cas (status `MAXEVAL_REACHED`, loglik nettement pire que le nouveau chemin EM-split) — signe que séparer VE-step/M-step est non seulement plus cohérent avec l'architecture existante mais aussi plus robuste numériquement que d'optimiser $\rho$ conjointement avec des milliers de paramètres $(M,S,\Theta)$ via nlopt.
- Suite complète : 979 PASS, 0 FAIL (ajout purement additif à `covariance_pln.h`).

### Reste à faire (reporté, explicitement hors scope de cette entrée)

Aucun câblage R (pas de `covariance="genetic"` dans `PLN_param()`, pas de classe `PLNfit_genetic`, pas de tests, pas de doc) — demandé explicitement par l'utilisateur pour une étape suivante.

---

## 58. Câblage R de la covariance genpop : classe `PLNfit_genpop` + `PLN()`/`PLN_param()` (17/06)

**Fichiers** : `R/PLNfit-class.R`, `R/PLN.R`, `R/ZIPLN.R`, `man/*.Rd`

### Contexte

L'utilisateur avait laissé un brouillon entièrement commenté de classe (`PLNfit_genetprior`, puis renommé en en-tête `PLNfit_genpop`) dans `PLNfit-class.R`, comme point de départ volontaire pour le câblage R — confirmé explicitement comme intentionnel. Demande : "tu peux t'appuyer dessus pour commencer `PLNfit_genpop`", puis "continue avec PLN / PLN_param".

### Classe `PLNfit_genpop`

Écrite sur le modèle de `PLNfit_fixedcov` (le sibling le plus proche : a aussi besoin d'une quantité externe fixe — `C` au lieu de `Omega`) plutôt que de suivre littéralement le brouillon (qui appelait une fonction placeholder `nlopt_optimizexxx` et passait les arguments à plat, antérieur au refactor `(data,params,config)`/`setup_optimizer`) :
- `initialize()` : valide `control$C` (dimensions p×p), le stocke dans `private$C`, branche `{builtin,nlopt}_optimize_genetic`/`{builtin,nlopt}_optimize_vestep_genetic` via `setup_optimizer` (déjà ajoutés à l'entrée 57).
- `optimize()` : identique à `PLNfit_fixedcov$optimize()` mais ajoute `C = private$C` dans `params` (pas de normalisation particulière pour `C`, c'est une matrice de corrélation fixe, pas une covariable).
- `gen_par` (active binding) : décode `(sigma2, rho)` depuis `Sigma` et `C` par régression linéaire (`qr.solve` sur `vec(Sigma) = a*vec(C) + b*vec(I)`, puis `sigma2=a+b, rho=a/sigma2`) — évite de toucher `make_pln_result`/le C++ partagé pour exposer ces deux scalaires, alors que `GeneticCovTraits::output_cov` les calculait déjà mais `make_pln_result` ne les transmet pas (limitation connue, notée dans la mémoire de l'entrée 57).
- `vcov_model="genpop"`, `nb_param = p*d + 2`. Pas de redéfinition de `optimize_vestep`/`predict`/`predict_cond` : la version générique de `PLNfit` marche telle quelle (le VE-step n'a besoin que d'un `Omega` dense, déjà ce que fournit `nlopt_optimize_vestep_genetic`/`builtin_optimize_vestep_genetic`).

**Bug de syntaxe pendant l'écriture** : parenthèse fermante de l'appel `R6Class(...)` manquante en fin de bloc (oubliée après le bloc `active = list(...)`) — détecté immédiatement par les diagnostics de l'IDE (`Unmatched opening delimiter`), corrigé en une ligne.

### Câblage `PLN()` / `PLN_param()`

- `PLN_param()` : ajout de `covariance = "genpop"` aux choix, nouveau paramètre `C = NULL`, validation `stopifnot(inherits(C, "matrix") | inherits(C, "Matrix"))` quand `covariance == "genpop"` (miroir exact du traitement existant de `Omega`/`"fixed"`), `C` ajouté à la liste retournée.
- `PLN()` : nouvelle branche `"genpop" = PLNfit_genpop$new(...)` dans le `switch(control$covariance, ...)`.
- Portée volontairement limitée à `PLN()` seul (pas PLNPCA/PLNnetwork/PLNmixture/PLNLDA) : vérifié par grep que `"fixed"` lui-même n'est déjà disponible que pour `PLN()`/`ZIPLN()` parmi tous les `*_param()` — `"genpop"` suit la même portée que `"fixed"`.

**Effet de bord découvert et corrigé** : `ZIPLN_param()` utilise `@inheritParams PLN_param` (roxygen) pour le paramètre `covariance` — modifier le texte de doc de `PLN_param()` aurait fait apparaître "genpop" dans la doc de `ZIPLN_param()`, qui ne le supporte pas (et qui, à l'inverse, supporte "sparse", jamais mentionné car absent du texte source de PLN_param — incohérence préexistante, non corrigée car hors scope). Corrigé en ajoutant un `@param covariance` explicite dans le bloc roxygen de `ZIPLN_param()` (R/ZIPLN.R), qui prend le pas sur celui hérité.

### Validation

- `PLN(Y ~ 1, data=df, control=PLN_param(covariance="genpop", C=Cmat))` : fit complet, `print()`, `predict()` niveau 0 et niveau 1 (VE-step réutilisé) fonctionnent via le pipeline standard.
- Erreur correctement levée si `C` absent avec `covariance="genpop"`.
- `devtools::document()` : `PLNfit_genpop.Rd` généré, `PLN_param.Rd` mis à jour, `ZIPLN_param.Rd` corrigé (pas de fuite de "genpop" dans sa doc).
- Suite complète : 979 PASS, 0 FAIL.

---

## 59. Tests dédiés `test-pln-genpop.R` (17/06)

**Fichier** : `tests/testthat/test-pln-genpop.R` (nouveau), sur le modèle de `test-pln.R`/`test-plnfit.R` (cf. le test existant de `covariance="fixed"` dans `test-plnfit.R` pour le style), avec `trichoptera` (p=17, n=49) et une matrice de corrélation AR1 synthétique (`0.5^|i-j|`, diagonale unité) comme `C`.

7 `test_that` : validation `PLN_param(covariance="genpop")` exige `C` (matrice) ; fit de base builtin et nlopt ; `vcov_model`/`nb_param` (`p*d+2`) ; accord builtin/nlopt (loglik tolérance 1e-1, `gen_par$sigma2`/`gen_par$rho` tolérance 0.2 — desserrée après un premier run : sur un aussi petit jeu de données les deux backends s'accordent sur le loglik mais pas au point près sur `(sigma2,rho)` individuellement, cohérent avec la marge déjà tolérée pour le loglik) ; `gen_par` renvoie `sigma2>0` et `rho∈[0,1]` ; **loglik(genpop) ≤ loglik(full)** — garanti mathématiquement, pas juste empirique (l'ensemble des $\Sigma$ atteignables par genpop est un sous-ensemble strict de celles atteignables par full, à $(M,S)$ optimaux respectifs) ; `predict()` niveaux 0 et 1.

Suite complète : 1004 PASS (979 + 25 nouveaux), 0 FAIL.

---

## 60. Préparation du merge `code-enhancement` → `master` (17/06)

**Fichiers** : `NEWS.md`, `inst/benchmark/branch_comparison.csv`

### Vérifications de préparation

- Suite complète : 979 PASS, 0 FAIL sur `code-enhancement` seul (sans les commits `genetic-cov`, restés sur leur branche dédiée — confirmé non mélangés).
- `devtools::document()` : aucun diff (man/NAMESPACE déjà synchronisés).
- `DESCRIPTION` : dépendances cohérentes (`future`/`future.apply`/`pscl` bien retirées des Imports).
- Aucun code de debug oublié dans les fichiers touchés par les 27 commits depuis le merge-base (`9221b6f4`).

### Décision sur `config_optim$profiled`

Rappel explicite à l'utilisateur du statut provisoire (cf. mémoire `project-pln-profiled-full`) avant le merge — question posée directement (garder TRUE / repasser FALSE / isoler le commit). **Décision : garder `TRUE` et merger.** Documenté dans `NEWS.md` comme défaut définitif (plus de réserve "provisoire" à rappeler dans les futures sessions).

### NEWS.md complété (version inchangée, `1.3.0-9010`)

Nouvelle section "Internal refactoring (C++)" (abstraction CRTP partagée Full/Diagonal/Spherical/Fixed + réutilisation PLNPCA/ZIPLN, convention de nommage unifiée des exports), bullet `profiled=TRUE`, bullet fix PLNnetwork/ZIPLNnetwork inception (grille de pénalité fausse, −311 ll oaks — déjà fait plus tôt dans la session, juste documenté maintenant).

### Rafraîchissement du benchmark master vs code-enhancement

Les fichiers committés (`branch_comparison.csv` etc.) dataient du merge-base (15-16/06), donc d'avant le flip `profiled` et la restructuration ZIPLN/PLNPCA. Relancé `run_branch.R` pour les deux branches (master installé via `git worktree add /tmp/PLNmodels-master master` + `R CMD INSTALL` dans des libs séparées, séquentiellement comme l'exige le script) puis `compare_branches.R`.

**Résultats** (24 runs/branche, trichoptera/barents/oaks) :
- PLN nlopt (effet direct du flip `profiled`) : loglik et nb itérations nettement meilleurs sur les 3 datasets (ex. oaks : -32048.5→-32030.0, itérations 1738→598) — confirme une nouvelle fois le bénéfice du flag, cette fois en conditions de benchmark "officielles" pré-merge.
- ZIPLN oaks : gain spectaculaire sur les deux backends (builtin -32680.7→-32097.3, nlopt -33351.4→-32090.0, +583 à +1261 ll) — cohérent avec tout le travail de portage CRTP/nettoyage de cette session.
- PLNPCA : quasiment inchangé (builtin bit-identique, nlopt légèrement meilleur) — attendu, pur renommage côté C++.
- **PLNnetwork (oaks, trichoptera) : loglik apparemment *pire*** sur le "meilleur modèle par BIC" (-1376 ll sur oaks). Investigué : ce n'est **pas une régression de qualité** mais un artefact de grille — `max_pen` calculé par le nouvel init ("plus simple et meilleur", déjà documenté) est ~2× plus grand que celui de master, donc la grille à `n_penalties=10` ne descend pas aussi bas en sparsité. Vérifié en forçant `code-enhancement` à explorer la même valeur extrême de pénalité que master (λ=1.136) : loglik *légèrement meilleur* que master au même point (-37593.7 vs -37603.2). Pas d'action corrective nécessaire, juste une note d'interprétation.
- `inst/benchmark/branch_comparison.csv` mis à jour avec ces résultats frais ; `compare_backends_results.{csv,rds}` et les 3 PDF (issus de `compare_backends.R`, un script plus large sur 6 datasets incluant mollusk/microcosm/scRNA) **non rafraîchis** — hors du périmètre demandé pour ce merge, à voir séparément si besoin.
- Nettoyage : worktree temporaire `/tmp/PLNmodels-master` et libs temporaires supprimés après usage.

---

## 61. Révision des backends par défaut (PLN, PLNPCA, ZIPLN, PLNnetwork) (18/06)

**Fichiers** : `R/PLN.R`, `R/PLNnetwork.R`, `man/PLN_param.Rd`, `man/PLNnetwork_param.Rd`, `tests/testthat/test-pln.R`, `tests/testthat/test-plnfit.R`, `NEWS.md`

Demande explicite de l'utilisateur : proposer les backends par défaut à partir des derniers résultats de benchmark (`inst/benchmark/branch_comparison.csv` rafraîchi à l'entrée 60), pour PLN/PLNPCA/ZIPLN/PLNnetwork.

### Analyse

- **PLN** (full covariance) : avec `profiled=TRUE`, nlopt est maintenant systématiquement plus rapide que builtin sur trichoptera/barents/oaks (×2-4), à loglik quasi identique. Test complémentaire ciblé sur microcosm (n=880, p=259, non couvert par `branch_comparison.csv`) : builtin 109.6s/ll=-217553.2 vs nlopt 32.0s/ll=-217649.1 — nlopt ×3.4 plus rapide mais −96 de loglik, écart qui n'est plus négligeable. Compromis vitesse/qualité qui croît avec p.
- **PLNPCA** : mon test rapide montre builtin souvent légèrement meilleur en loglik, mais c'est déjà un choix délibéré documenté dans `R/PLNPCA.R` (nlopt gardé par prudence : builtin peut tomber dans un optimum local nettement pire sur des jeux mal conditionnés). Pas de changement proposé.
- **ZIPLN** : builtin gagne nettement sur trichoptera, compétitif sur oaks. Pas de changement.
- **PLNnetwork** : découverte que `R/PLNnetwork.R` documentait déjà (commit `38f03119`, déjà mergé) une combinaison benchmarkée — `backend="builtin", maxit_ve=1, inception_backend="builtin", inception_niter=5` — donnant un gain systématique de loglik (+34/+123/+676 sur trichoptera/barents/oaks) à vitesse quasi identique, **jamais câblée comme défaut**.

Présenté à l'utilisateur sous forme de tableau ; décision : garder PLNPCA/ZIPLN inchangés, appliquer le changement PLNnetwork, et **repasser PLN à `nlopt`** par défaut (l'utilisateur privilégie le gain de vitesse, en gardant `builtin` disponible et documenté pour les cas où la qualité de l'optimum prime).

### Changements appliqués

- `PLN_param()` : `backend = c("nlopt", "builtin", "torch")` (était `c("builtin", ...)`). Doc mise à jour pour expliquer le compromis (nlopt plus rapide grâce à `profiled`, builtin meilleur optimum à grand p).
- `PLNnetwork_param()` : `backend = c("builtin", "nlopt", "torch")` (était `c("nlopt", ...)`) ; `maxit_ve`/`inception_niter` restent `NULL` par défaut dans la signature mais sont remplis à `1L`/`5L` à l'intérieur de la fonction **uniquement si `backend == "builtin"` et que l'utilisateur ne les a pas fournis explicitement** — `inception_backend` reste `NULL` (hérite déjà de `backend` via la logique existante dans `PLNnetworkfamily-class.R`, pas besoin de dupliquer le défaut).
- 2 tests cassés par le changement de défaut PLN (`expect_output` codant en dur "builtin optimizer" dans le message de trace) : `test-pln.R:63`, `test-plnfit.R:12,21` — corrigés pour attendre "nlopt optimizer".
- `NEWS.md` : bullet existant "New built-in Newton optimizer... now the default for PLN and ZIPLN" corrigé (retrait de "PLN", garde "ZIPLN" qui reste inchangé) ; 2 nouveaux bullets ajoutés documentant les deux changements de défaut.

### Validation

- `devtools::document()` : 2 fichiers `.Rd` régénérés sans surprise.
- Suite complète : 1004 PASS, 0 FAIL (après correction des 2 tests).

---

## 62. README.Rmd → README.qmd, avec illustrations exécutées sur barents (18/06)

**Fichiers** : `README.qmd` (nouveau), `README.Rmd` (supprimé), `README.md`, `.Rbuildignore`, `man/figures/README-*.png`

Demande utilisateur : remplacer le README placeholder (tous les chunks en `eval=FALSE`) par un document Quarto réellement exécuté, illustratif, concis, avec du cache. Hésitation initiale sur le jeu de données : `oaks` (n=116, p=114) d'abord essayé puis abandonné par l'utilisateur car trop lourd pour une démo copier-coller rapide (PLNnetwork seul prenait 63s) ; basculé sur `barents` (n=89, p=30), nettement plus rapide (PLN 0.7s, LDA 1.5s, PCA 2.8s, network ~4s) grâce aussi aux nouveaux défauts de backend (cf. entrée 61).

5 démos avec une figure chacune : PLN (corrplot des corrélations), PLNLDA (séparation nette North/South sur un découpage de Latitude, axe à 99.3% de variance expliquée), PLNPCA (biplot factoextra coloré par température), PLNnetwork (réseau épars ~33 arêtes/30 noeuds via `getBestModel` + `remove.isolated=TRUE`), PLNmixture (clustering k=4 sélectionné par ICL, `smoothing="none"` pour rester rapide). Stability selection et smoothing PLNmixture délibérément exclus (coûteux, demande explicite).

### Bug fantôme : cache Quarto + objets R6

Premier rendu : crash sur le chunk `plnpca` avec `Error in private$M - private$X %*% private$B : non-conformable arrays`, trace passant par `lazyLoadDBinsertValue`. L'utilisateur a immédiatement proposé une piste concrète et plausible (M dans PLNPCA n'a pas la même sémantique que dans PLN — n×rank au lieu de n×p — donc `latent_pos` du chunk de base ne devrait pas s'appliquer). Vérifié : `PLNPCAfit-class.R:548` **surcharge déjà correctement** `latent_pos` (`tcrossprod(private$M, private$C)`, donne bien n×p) ; testé `myPCA$latent_pos` directement → dimensions correctes (89×30), aucune erreur en exécution interactive isolée du même code. La trace `lazyLoadDBinsertValue` est la signature classique du mécanisme de **cache lazy de knitr** (objets sauvés individuellement via promesses) qui ne survit pas à des objets R6 (environnements, bindings actifs) — la sérialisation du chunk, pas l'analyse elle-même, déclenchait une évaluation erronée. Fix : `cache.lazy = FALSE` dans `knitr::opts_chunk$set()` (force un seul `save()`/`load()` par chunk au lieu de promesses individuelles). Re-rendu propre après suppression du cache existant.

**Why:** piège connu mais non documenté dans ce repo : ne jamais cacher des chunks retournant des objets R6 (PLNfit, PLNPCAfit, etc.) sans `cache.lazy = FALSE`.

### Nettoyage

- `README.Rmd` supprimé, `.Rbuildignore` mis à jour (`README.Rmd` → `README.qmd` + `README_cache`/`README_files`).
- 6 figures orphelines pré-Quarto supprimées (`man/figures/PLN-1.png`, `PLNLDA-1.png`, `PLNPCA-{1,2}.png`, `PLNnetwork-{1,2}.png`) — aucune référence trouvée ailleurs dans le repo.
- Bibliographie complétée : PLNLDA (Fisher 1936, Rao 1948), graphical lasso (FHT08) pour PLNnetwork, model-based clustering (Fraley & Raftery 1999) pour PLNmixture, et citation du jeu de données barents (Fossheim et al. 2006).

---

## 63. Nouvelle vignette ZIPLN/ZIPLNnetwork (18/06)

**Fichiers** : `vignettes/ZIPLN.Rmd` (nouveau), `_pkgdown.yml`, `vignettes/article/PLNreferences.bib`

Demande utilisateur : une vignette sur `ZIPLN`/`ZIPLNnetwork`, sur un sous-ensemble de variables de `microcosm` (n=880, p=259, ~90% de zéros), illustrative et rapide à compiler.

- Sous-ensemble retenu : les **30 taxa les plus abondants** (par somme des comptages) — garde un taux de zéros substantiel (~80%) tout en restant rapide (chaque fit < 10s). `microcosm$Offset` est un vecteur par échantillon (pas une matrice par taxon comme `oaks`/`barents`) — piège évité car repéré avant d'écrire le code définitif (`Offset[, top]` aurait échoué).
- Contenu : comparaison PLN vs 4 paramétrisations ZIPLN (`single`/`row`/`col`/dépendant du site via `| site`) sur loglik/BIC/ICL — le modèle dépendant du site gagne nettement sur les deux critères ; fitted vs observé ; boxplot des probabilités de zero-inflation estimées (`model_par$Pi`) par site corporel (nasal nettement moins zero-inflaté que vaginal/oral/lait — résultat interprétable) ; `ZIPLNnetwork` avec diagnostic + sélection BIC + plot du réseau épars (17 arêtes/30 noeuds, propre visuellement). `stability_selection` mentionnée mais pas exécutée (cohérent avec la convention déjà utilisée pour PLNnetwork.Rmd/README).
- Découverte en cours de route : une étude de cas exploratoire préexistante `inst/case_studies/microcosm.qmd` traite déjà ZIPLN sur microcosm (17 paramétrisations site/time, dépendances lourdes hors package) — confirme la pertinence du choix de dataset, mais reste hors-scope (exploratoire, pas une vignette polie), pas de modification.
- Entrée bib ajoutée : `@microcosm` (Mariadassou et al. 2023, déjà citée dans `man/microcosm.Rd`). Entrée pkgdown ajoutée dans le menu Articles.
- Rendu testé via `rmarkdown::render()` : 23 chunks, 0 erreur.
